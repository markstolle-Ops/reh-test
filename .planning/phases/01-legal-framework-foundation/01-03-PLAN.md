---
phase: 01-legal-framework-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/calculator.ts
  - src/lib/calculator.test.ts
  - src/lib/states.ts
  - src/components/calculator/savings-calculator.tsx
  - src/components/calculator/savings-chart.tsx
  - src/components/calculator/breakdown-table.tsx
  - src/app/page.tsx
  - docs/legal/ai-guidance-taxonomy.md
  - docs/legal/state-compliance-classification.md
autonomous: true
requirements:
  - COST-01
  - COST-02

must_haves:
  truths:
    - "Homepage displays a cost-benefit calculator"
    - "Calculator accepts a home price and state as input"
    - "Calculator shows itemized savings compared to traditional 5-6% commission"
    - "Calculator shows a bar chart comparing traditional agent cost vs platform cost"
    - "Attorney-required states show attorney fee in the breakdown"
    - "AI guidance taxonomy document defines permitted vs prohibited AI guidance"
    - "State compliance classification covers all 10 launch states"
  artifacts:
    - path: "src/lib/calculator.ts"
      provides: "Pure calculation functions for commission savings"
      exports: ["calculateSavings"]
      min_lines: 40
    - path: "src/lib/calculator.test.ts"
      provides: "Unit tests for calculator logic"
      contains: "calculateSavings"
    - path: "src/components/calculator/savings-calculator.tsx"
      provides: "Main calculator client component with inputs"
      contains: "use client"
    - path: "src/components/calculator/savings-chart.tsx"
      provides: "Recharts bar chart comparing costs"
      contains: "BarChart"
    - path: "docs/legal/ai-guidance-taxonomy.md"
      provides: "AI guidance boundary document"
      contains: "Permitted"
    - path: "docs/legal/state-compliance-classification.md"
      provides: "10 launch state legal classification"
      contains: "attorney"
  key_links:
    - from: "src/components/calculator/savings-calculator.tsx"
      to: "src/lib/calculator.ts"
      via: "calculateSavings() called on input change"
      pattern: "calculateSavings"
    - from: "src/components/calculator/savings-chart.tsx"
      to: "recharts"
      via: "BarChart rendering breakdown data"
      pattern: "BarChart"
    - from: "src/lib/calculator.ts"
      to: "src/lib/constants.ts"
      via: "PLATFORM_FEE_PLACEHOLDER and ATTORNEY_STATES imports"
      pattern: "PLATFORM_FEE_PLACEHOLDER"
---

<objective>
Build the cost-benefit calculator (homepage component showing itemized savings vs traditional commission) and produce the legal framework documents (AI guidance taxonomy + state compliance classification for 10 launch states).

Purpose: The calculator is the primary conversion tool on the homepage — it shows prospective users exactly how much they save. The legal documents define the AI guidance boundary that every future AI prompt must respect.
Output: Working calculator on homepage + two legal governance documents in docs/legal/.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legal-framework-foundation/01-RESEARCH.md
@.planning/phases/01-legal-framework-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From plan 01-01 outputs -->

From src/types/index.ts:
```typescript
export type UserRole = "buyer" | "seller";
export type LaunchState = "CA" | "TX" | "FL" | "NY" | "GA" | "NC" | "AZ" | "OH" | "PA" | "IL";
export type ClosingType = "title-company" | "attorney-required" | "customary-attorney";
export interface CommissionBreakdown {
  homePrice: number;
  state: string;
  traditionalCommission: number;
  platformFee: number;
  titleFee: number;
  attorneyFee: number;
  totalWithPlatform: number;
  totalWithAgent: number;
  savings: number;
}
```

From src/lib/constants.ts:
```typescript
export const PLATFORM_FEE_PLACEHOLDER = 2500;
export const LAUNCH_STATES = ["CA", "TX", "FL", "NY", "GA", "NC", "AZ", "OH", "PA", "IL"];
export const ATTORNEY_STATES = ["GA", "NC"];
export const CUSTOMARY_ATTORNEY_STATES = ["NY", "IL"];
export const COMMISSION_RATE_DEFAULT = 0.055;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calculator logic with unit tests (TDD)</name>
  <files>src/lib/calculator.ts, src/lib/calculator.test.ts, src/lib/states.ts</files>
  <behavior>
    - calculateSavings(400000, "CA") returns savings > 0, traditionalCommission = 22000, platformFee = 2500, attorneyFee = 0
    - calculateSavings(400000, "GA") returns attorneyFee = 1500 (attorney-required state)
    - calculateSavings(400000, "NY") returns attorneyFee = 1500 (customary-attorney state)
    - calculateSavings(0, "CA") returns all zeros (edge case)
    - calculateSavings(1000000, "TX") returns savings proportional to home price
    - estimateTitleFee returns a positive number for any valid state + price
    - getStateInfo("CA") returns { code: "CA", name: "California", closingType: "title-company" }
    - getStateInfo("GA") returns { closingType: "attorney-required" }
  </behavior>
  <action>
    RED: Write `src/lib/calculator.test.ts` first with all behavior cases above using Vitest. Tests import from `@/lib/calculator` and `@/lib/states`. Run `npx vitest run src/lib/calculator.test.ts` — all tests MUST fail (files don't exist yet).

    GREEN: Implement `src/lib/states.ts`:
    - Export `STATE_INFO` object: a record mapping each launch state code to `{ code, name, closingType, estimatedTitleFeePercent }`. Classification per research:
      - Title company: CA, TX, FL, AZ, OH, PA
      - Attorney required: GA, NC
      - Customary attorney: NY, IL
    - Export `getStateInfo(stateCode: string)` function

    Implement `src/lib/calculator.ts`:
    - Import `PLATFORM_FEE_PLACEHOLDER`, `COMMISSION_RATE_DEFAULT` from constants
    - Import `STATE_INFO` from states
    - Export `estimateTitleFee(homePrice: number, state: string): number` — approximate as 0.5-1% of home price varying by state
    - Export `calculateSavings(homePrice: number, state: string, commissionRate?: number): CommissionBreakdown` — per the interface in types/index.ts
    - Attorney fee: $1,500 for attorney-required AND customary-attorney states, $0 for title-company states
    - Platform fee: use PLATFORM_FEE_PLACEHOLDER constant (do NOT hardcode $2500 in calculator.ts)

    Run tests again — all MUST pass.

    REFACTOR if needed: ensure all pure functions, no side effects, no imports of React or DOM.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/lib/calculator.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All calculator unit tests pass
    - calculateSavings returns correct breakdown for attorney vs non-attorney states
    - STATE_INFO covers all 10 launch states with correct closing types
    - Platform fee uses constant, not hardcoded value
  </done>
</task>

<task type="auto">
  <name>Task 2: Calculator UI components + homepage integration + legal docs</name>
  <files>
    src/components/calculator/savings-calculator.tsx,
    src/components/calculator/savings-chart.tsx,
    src/components/calculator/breakdown-table.tsx,
    src/app/page.tsx,
    docs/legal/ai-guidance-taxonomy.md,
    docs/legal/state-compliance-classification.md
  </files>
  <action>
    1. Install shadcn/ui components needed: `npx shadcn@latest add input select card separator`

    2. Create `src/components/calculator/savings-calculator.tsx` — "use client" component. Contains:
    - Numeric input for home price (formatted with commas, e.g., "$400,000"). Use a controlled input with state.
    - State dropdown (shadcn Select) populated from LAUNCH_STATES constant. Default to "CA".
    - On any input change, call `calculateSavings()` and pass result to chart + breakdown.
    - Wrap the chart and breakdown components below the inputs.

    3. Create `src/components/calculator/savings-chart.tsx` — "use client" component (Recharts requires it). Receives `CommissionBreakdown` as prop. Renders a `BarChart` with two bars:
    - "Traditional Agent" (red, #ef4444) showing totalWithAgent
    - "RealEstateHunter" (green, #22c55e) showing totalWithPlatform
    - XAxis with labels, YAxis with $Xk formatting, Tooltip with full dollar formatting
    - Wrap in `ResponsiveContainer` for responsive sizing

    4. Create `src/components/calculator/breakdown-table.tsx` — "use client" component. Receives `CommissionBreakdown` as prop. Renders an itemized comparison table:
    - Row: "Agent Commission (5.5%)" showing traditionalCommission | "Platform Fee" showing platformFee with note "(approximate — exact pricing coming soon)"
    - Row: "Title/Escrow Fees" showing titleFee | same
    - Row: "Attorney Fee" showing attorneyFee (or "N/A" for non-attorney states) | same
    - Row: "Total" bold showing totalWithAgent | totalWithPlatform
    - Row: "Your Savings" highlighted showing savings amount
    - Format all numbers as `$XX,XXX` with `toLocaleString()`

    5. Update `src/app/page.tsx` — add the SavingsCalculator component to the homepage below the hero section. Add a heading "See How Much You Could Save" above the calculator. Keep the page as a Server Component — the calculator is a client component island within it.

    6. Create `docs/legal/ai-guidance-taxonomy.md` — the AI guidance boundary document per Pattern 4 from research. Sections:
    - **Permitted AI Guidance (Safe Harbor):** list of example permitted responses (process info, form guidance, comparable data, general next steps)
    - **Prohibited AI Guidance (UPL Risk):** list of prohibited patterns (legal entitlements, contract interpretation, enforceability opinions, litigation advice)
    - **Mandatory Disclaimer Language:** exact text every AI response on transaction topics must include
    - **Attorney Referral Trigger Conditions:** list of user question patterns that trigger attorney referral
    - **Review Status:** "DRAFT — Requires real estate attorney review before any AI prompt is written. Do not treat as authoritative until reviewed."

    7. Create `docs/legal/state-compliance-classification.md` — classification for 10 launch states. Include a table with columns: State, Code, Closing Type, Attorney Required, Agent Required, FSBO Allowed, Notes. Data from research's Launch State Classification table. Include:
    - Header noting this is for the 10 launch states
    - Classification definitions (title-company, attorney-required, customary-attorney)
    - Workflow implications for Phase 4
    - Review status: "DRAFT — Requires attorney review for each classified state"

    Anti-patterns to avoid:
    - Do NOT render Recharts in a Server Component — will throw "window is not defined"
    - Do NOT hardcode $2,500 in the UI — import PLATFORM_FEE_PLACEHOLDER from constants
    - Do NOT calculate on the server — pure client-side math, no API round-trip
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Homepage shows the cost-benefit calculator with price input and state selector
    - Changing price or state updates the chart and breakdown in real-time
    - Attorney-required states show attorney fee line item; non-attorney states show "N/A"
    - Bar chart renders two bars (traditional vs platform)
    - docs/legal/ai-guidance-taxonomy.md exists with all required sections and DRAFT status
    - docs/legal/state-compliance-classification.md exists with all 10 launch states classified and DRAFT status
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/calculator.test.ts` — all calculator unit tests pass
- `npx next build` — app builds with calculator component on homepage
- Calculator renders on `/` with working inputs
- Selecting "GA" or "NC" shows attorney fee in breakdown
- Selecting "CA" shows $0 attorney fee
- Legal docs exist in docs/legal/ with required sections and DRAFT status
</verification>

<success_criteria>
- Cost-benefit calculator on homepage accepts home price + state, shows itemized savings (COST-01, COST-02)
- Bar chart visually compares traditional vs platform costs
- Itemized breakdown table shows commission, title fees, attorney fees, total, savings
- Calculator uses PLATFORM_FEE_PLACEHOLDER constant (single source of truth)
- All calculator unit tests pass
- AI guidance taxonomy document exists with permitted/prohibited/disclaimer/trigger sections
- State compliance classification document exists for all 10 launch states
</success_criteria>

<output>
After completion, create `.planning/phases/01-legal-framework-foundation/01-03-SUMMARY.md`
</output>
