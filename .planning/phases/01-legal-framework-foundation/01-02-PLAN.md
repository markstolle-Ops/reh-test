---
phase: 01-legal-framework-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(buyer)/dashboard/page.tsx
  - src/app/(buyer)/layout.tsx
  - src/app/(seller)/dashboard/page.tsx
  - src/app/(seller)/layout.tsx
  - src/app/onboarding/page.tsx
  - src/components/layout/role-switcher.tsx
  - src/components/layout/nav.tsx
  - src/lib/auth.ts
  - src/lib/__tests__/role-check.test.ts
  - tests/auth/signup.spec.ts
  - tests/auth/verify-email.spec.ts
  - tests/auth/password-reset.spec.ts
  - tests/auth/session-persistence.spec.ts
  - tests/auth/role-dashboards.spec.ts
  - tests/auth/role-switch.spec.ts
autonomous: true
requirements:
  - ACCT-02
  - ACCT-03
  - ACCT-05
  - ACCT-06

must_haves:
  truths:
    - "User receives email verification after signup (Clerk built-in)"
    - "User can reset password via email link (Clerk built-in)"
    - "Buyer sees a buyer-specific dashboard at /buyer/dashboard"
    - "Seller sees a seller-specific dashboard at /seller/dashboard"
    - "User can switch between buyer and seller roles and is redirected to the correct dashboard"
    - "Unauthenticated user accessing /buyer/* or /seller/* is redirected to /sign-in"
  artifacts:
    - path: "src/app/(buyer)/dashboard/page.tsx"
      provides: "Buyer dashboard page"
      contains: "Buyer Dashboard"
    - path: "src/app/(seller)/dashboard/page.tsx"
      provides: "Seller dashboard page"
      contains: "Seller Dashboard"
    - path: "src/components/layout/role-switcher.tsx"
      provides: "Client component for switching buyer/seller role"
      contains: "switchRole"
    - path: "src/app/onboarding/page.tsx"
      provides: "Post-signup role selection page"
      contains: "buyer"
  key_links:
    - from: "src/components/layout/role-switcher.tsx"
      to: "src/lib/auth.ts"
      via: "Server action call to setUserRole"
      pattern: "setUserRole"
    - from: "src/components/layout/role-switcher.tsx"
      to: "@clerk/nextjs"
      via: "user.reload() after role switch"
      pattern: "user.*reload"
    - from: "src/app/(buyer)/dashboard/page.tsx"
      to: "@clerk/nextjs/server"
      via: "currentUser() role check"
      pattern: "currentUser"
---

<objective>
Implement buyer and seller dashboards with role-specific views, post-signup onboarding (role selection), and role switching. Clerk handles email verification (ACCT-02) and password reset (ACCT-03) out of the box — this plan verifies they work and builds the role-specific UI layer on top.

Purpose: Users need role-aware dashboards and the ability to switch roles so the platform serves both buyers and sellers from a single account.
Output: Working buyer/seller dashboards, role switcher component, onboarding page, and full E2E test scaffold covering all ACCT requirements.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legal-framework-foundation/01-RESEARCH.md
@.planning/phases/01-legal-framework-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From plan 01-01 outputs that this plan depends on -->

From src/types/index.ts:
```typescript
export type UserRole = "buyer" | "seller";
```

From src/lib/auth.ts:
```typescript
export async function setUserRole(userId: string, role: UserRole): Promise<void>;
```

From src/middleware.ts:
```typescript
// Already protects /buyer(.*)  and /seller(.*)  routes
// Reads sessionClaims.role for enforcement
// REQUIRES Clerk Dashboard JWT template: { "role": "{{user.public_metadata.role}}" }
```

From src/lib/constants.ts:
```typescript
export const LAUNCH_STATES: string[];
export const ATTORNEY_STATES: string[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Buyer and seller dashboards with role routing + onboarding + role-check helper</name>
  <files>
    src/app/(buyer)/dashboard/page.tsx, src/app/(buyer)/layout.tsx,
    src/app/(seller)/dashboard/page.tsx, src/app/(seller)/layout.tsx,
    src/app/onboarding/page.tsx, src/components/layout/nav.tsx,
    src/lib/role-check.ts, src/lib/__tests__/role-check.test.ts,
    tests/auth/role-dashboards.spec.ts
  </files>
  <action>
    1. Create `src/lib/role-check.ts` — a pure helper function `getUserDashboardPath(role: string | undefined): string` that returns `/buyer/dashboard` for "buyer", `/seller/dashboard` for "seller", and `/onboarding` for undefined/unknown roles. Also export `isValidRole(role: unknown): role is UserRole` type guard. These are pure functions with no Clerk dependency — fully unit-testable.

    2. Create `src/lib/__tests__/role-check.test.ts` — Vitest unit tests for the role-check helpers:
    - `getUserDashboardPath("buyer")` returns `/buyer/dashboard`
    - `getUserDashboardPath("seller")` returns `/seller/dashboard`
    - `getUserDashboardPath(undefined)` returns `/onboarding`
    - `getUserDashboardPath("admin")` returns `/onboarding`
    - `isValidRole("buyer")` returns `true`
    - `isValidRole("seller")` returns `true`
    - `isValidRole("admin")` returns `false`
    - `isValidRole(undefined)` returns `false`

    3. Create `src/app/(buyer)/layout.tsx` — a layout for the buyer route group. Import `currentUser()` from @clerk/nextjs/server. Check `user.publicMetadata.role === "buyer"`, redirect to /sign-in if not. Render a nav bar (from src/components/layout/nav.tsx) + children.

    4. Create `src/app/(buyer)/dashboard/page.tsx` — Server Component. Call `currentUser()`, display "Buyer Dashboard" heading, show user's name/email, and a placeholder section "Your Saved Searches" and "Your Favorites" (empty state UI for now — these features come in Phase 3). Include the RoleSwitcher component.

    5. Create `src/app/(seller)/layout.tsx` — same pattern as buyer layout but checks `role === "seller"`.

    6. Create `src/app/(seller)/dashboard/page.tsx` — Server Component. Call `currentUser()`, display "Seller Dashboard" heading, show user's name/email, and placeholder sections "Your Listings" and "Transaction History" (empty state). Include the RoleSwitcher component.

    7. Create `src/components/layout/nav.tsx` — shared navigation component. Show app name "RealEstateHunter", conditional links based on auth state (sign-in/sign-up if logged out, dashboard link if logged in). Use Clerk's `<UserButton />` for the profile dropdown when authenticated.

    8. Create `src/app/onboarding/page.tsx` — client component shown after first signup. Two large cards: "I'm a Buyer" and "I'm a Seller". Clicking one calls the `setUserRole` server action from `src/lib/auth.ts`, then calls `user.reload()` and redirects to the appropriate dashboard (`/buyer/dashboard` or `/seller/dashboard`). Style with shadcn/ui Card component — run `npx shadcn@latest add card button` if not already installed.

    9. Update `src/app/layout.tsx` to include the Nav component.

    10. Create `tests/auth/role-dashboards.spec.ts` — Playwright E2E test skeleton for ACCT-05. Test that a buyer-role user sees /buyer/dashboard and a seller-role user sees /seller/dashboard. Use Clerk testing helpers for authenticated sessions. Use `test.skip` guard when `CLERK_TESTING_TOKEN` is not set.

    Note on ACCT-02 (email verification) and ACCT-03 (password reset): These are Clerk built-in features enabled by default. No code is needed. Clerk's sign-up flow sends verification email automatically. Password reset is available from the Clerk sign-in component. These will be verified in E2E tests using Clerk test mode.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/lib/__tests__/role-check.test.ts --reporter=verbose && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - role-check unit tests pass (getUserDashboardPath and isValidRole)
    - /buyer/dashboard renders with "Buyer Dashboard" heading for buyer-role users
    - /seller/dashboard renders with "Seller Dashboard" heading for seller-role users
    - /onboarding shows role selection cards (buyer/seller)
    - Unauthenticated access to /buyer/* or /seller/* redirects to /sign-in
    - Nav component shows auth-aware links
    - tests/auth/role-dashboards.spec.ts exists with ACCT-05 test skeleton
  </done>
</task>

<task type="auto">
  <name>Task 2: Role switcher component + full E2E test scaffolds</name>
  <files>
    src/components/layout/role-switcher.tsx,
    tests/auth/signup.spec.ts,
    tests/auth/verify-email.spec.ts,
    tests/auth/password-reset.spec.ts,
    tests/auth/session-persistence.spec.ts,
    tests/auth/role-switch.spec.ts,
    tests/fixtures/clerk-helpers.ts
  </files>
  <action>
    1. Create `src/components/layout/role-switcher.tsx` — "use client" component. Uses `useUser()` from @clerk/nextjs to get current role from `user.publicMetadata.role`. Displays current role and a button/toggle to switch to the other role. On click: calls the `setUserRole` server action, then `user.reload()`, then `router.push()` to the new dashboard. Use shadcn/ui Button. Show loading state during the switch.

    Important: After calling `setUserRole`, the session token may be stale for up to 60 seconds. Calling `user.reload()` forces an immediate refresh. Do NOT skip the reload call — see Pitfall 1 in research.

    2. Create `tests/fixtures/clerk-helpers.ts` — helper utilities for Clerk E2E testing. Use Clerk's testing token approach: set `CLERK_TESTING_TOKEN` env var and use Clerk's `setupClerkTestingToken()` from `@clerk/testing/playwright`. Export helper functions for creating test sessions.

    3. Create `tests/auth/signup.spec.ts` — Playwright E2E test skeleton for ACCT-01. Navigate to /sign-up, verify the Clerk SignUp component renders. Mark with `test.skip` if CLERK_TESTING_TOKEN is not set (graceful degradation for CI without Clerk keys).

    4. Create `tests/auth/verify-email.spec.ts` — Playwright E2E test skeleton for ACCT-02. Verify that after signup, Clerk's email verification flow is triggered. In test mode, use Clerk's test API to confirm the verification email was queued. Mark with `test.skip` if CLERK_TESTING_TOKEN is not set. Include a comment: "Clerk handles email verification natively — this test confirms the flow is enabled, not custom code."

    5. Create `tests/auth/password-reset.spec.ts` — Playwright E2E test skeleton for ACCT-03. Navigate to /sign-in, click "Forgot password?" link in Clerk's SignIn component, verify the reset flow renders. Mark with `test.skip` if CLERK_TESTING_TOKEN is not set. Include a comment: "Clerk handles password reset natively — this test confirms the flow is accessible."

    6. Create `tests/auth/session-persistence.spec.ts` — Playwright E2E test skeleton for ACCT-04. Authenticate a test user, navigate to a protected route, reload the page, verify the user is still authenticated (not redirected to /sign-in). Mark with `test.skip` if CLERK_TESTING_TOKEN is not set.

    7. Create `tests/auth/role-switch.spec.ts` — Playwright E2E test skeleton for ACCT-06. Test that clicking the role switcher changes the role and redirects to the new dashboard.

    All E2E tests should use `test.skip` guards when Clerk testing tokens are unavailable, so `npx playwright test` does not fail in environments without Clerk configuration.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    - RoleSwitcher component renders current role and switch button
    - Role switch calls setUserRole server action + user.reload()
    - E2E test files exist for all ACCT requirements:
      - tests/auth/signup.spec.ts (ACCT-01)
      - tests/auth/verify-email.spec.ts (ACCT-02)
      - tests/auth/password-reset.spec.ts (ACCT-03)
      - tests/auth/session-persistence.spec.ts (ACCT-04)
      - tests/auth/role-dashboards.spec.ts (ACCT-05 — created in Task 1)
      - tests/auth/role-switch.spec.ts (ACCT-06)
    - All tests either pass or skip gracefully (no failures from missing Clerk config)
  </done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- `npx vitest run` passes (including role-check unit tests)
- `npx playwright test` passes (tests skip gracefully without Clerk tokens)
- /buyer/dashboard and /seller/dashboard render role-specific content
- Role switcher component switches roles and redirects correctly
- All 6 ACCT requirement test files exist in tests/auth/
</verification>

<success_criteria>
- Buyer dashboard at /buyer/dashboard with buyer-specific placeholder content
- Seller dashboard at /seller/dashboard with seller-specific placeholder content
- Onboarding page with buyer/seller role selection
- Role switcher component that changes role and redirects
- Role-check helper with passing unit tests (behavioral verification for ACCT-05)
- E2E test scaffolds for all auth flows (ACCT-01 through ACCT-06)
- Clerk handles email verification and password reset without custom code
</success_criteria>

<output>
After completion, create `.planning/phases/01-legal-framework-foundation/01-02-SUMMARY.md`
</output>
