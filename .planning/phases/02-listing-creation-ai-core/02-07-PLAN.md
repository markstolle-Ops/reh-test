---
phase: 02-listing-creation-ai-core
plan: 07
type: execute
wave: 4
depends_on: ["02-01", "02-03"]
files_modified:
  - src/components/fees/FeeBreakdown.tsx
  - src/components/fees/FeeBreakdown.test.tsx
  - src/services/fees/transaction-fees.ts
  - src/services/fees/transaction-fees.test.ts
  - src/services/mls/syndication.ts
  - src/app/listings/[id]/page.tsx
  - src/app/seller/listings/[id]/syndicate/page.tsx
autonomous: true
requirements: [COST-03, COST-04]

must_haves:
  truths:
    - "Every listing detail page shows a full fee breakdown: platform fee, title company fee, attorney fee (where required), agent-for-hire fee (where required)"
    - "Fee breakdown is visible before any user commits to a transaction"
    - "Fee amounts are state-aware (attorney fees only for attorney-required/customary-attorney states)"
  artifacts:
    - path: "src/components/fees/FeeBreakdown.tsx"
      provides: "Fee breakdown UI component showing all transaction fees"
      min_lines: 40
    - path: "src/services/fees/transaction-fees.ts"
      provides: "Transaction fee calculation service extending Phase 1 calculator"
      exports: ["calculateTransactionFees"]
    - path: "src/services/mls/syndication.ts"
      provides: "MLS syndication stub (placeholder for broker partner integration)"
      exports: ["submitToMls"]
  key_links:
    - from: "src/components/fees/FeeBreakdown.tsx"
      to: "src/services/fees/transaction-fees.ts"
      via: "calculateTransactionFees call"
      pattern: "calculateTransactionFees"
    - from: "src/app/listings/[id]/page.tsx"
      to: "src/components/fees/FeeBreakdown.tsx"
      via: "FeeBreakdown rendered on listing detail page"
      pattern: "FeeBreakdown"
---

<objective>
Build the fee transparency component (COST-03/04) showing full transaction fee breakdown on listing pages, and create the MLS syndication stub for future broker partner integration.

Purpose: Fee transparency is a core value proposition — showing users exactly what they pay versus traditional agents. MLS syndication stub keeps the architecture ready for broker partner integration.
Output: FeeBreakdown component integrated into listing pages, transaction fee service, MLS syndication stub.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-listing-creation-ai-core/02-RESEARCH.md
@.planning/phases/02-listing-creation-ai-core/02-01-SUMMARY.md
@.planning/phases/02-listing-creation-ai-core/02-03-SUMMARY.md

<interfaces>
From src/lib/calculator.ts (Phase 1):
```typescript
export function calculateSavings(homePrice: number, state: LaunchState): CommissionBreakdown;
export function estimateTitleFee(homePrice: number, state: LaunchState): number;
```

From src/lib/constants.ts (Phase 1):
```typescript
export const PLATFORM_FEE_PLACEHOLDER = 2500; // cents
export const ATTORNEY_STATES = ["GA", "NC"] as const;
export const CUSTOMARY_ATTORNEY_STATES = ["NY", "IL"] as const;
```

From src/lib/states.ts (Phase 1):
```typescript
export const STATE_INFO: Record<LaunchState, { closingType: ClosingType; titleFeePercent: number; }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transaction fee service + FeeBreakdown component with tests</name>
  <files>src/services/fees/transaction-fees.ts, src/services/fees/transaction-fees.test.ts, src/components/fees/FeeBreakdown.tsx, src/components/fees/FeeBreakdown.test.tsx</files>
  <behavior>
    - Test: calculateTransactionFees for CA (title-company state) returns platformFee + titleFee, no attorneyFee
    - Test: calculateTransactionFees for GA (attorney-required) includes attorneyFee of $1,500
    - Test: calculateTransactionFees for NY (customary-attorney) includes attorneyFee of $1,500
    - Test: calculateTransactionFees includes agentForHireFee placeholder (0 for now, populated in Phase 5)
    - Test: FeeBreakdown renders all fee line items for a GA listing
    - Test: FeeBreakdown hides attorney fee row for CA listing
    - Test: FeeBreakdown shows total and comparison to traditional commission
  </behavior>
  <action>
    1. Write tests FIRST for both service and component.

    2. Create src/services/fees/transaction-fees.ts:
       - Export interface `TransactionFees { platformFee: number; titleFee: number; attorneyFee: number; agentForHireFee: number; mlsSyndicationFee: number; totalFees: number; traditionalCommission: number; savings: number; }`
       - Export `calculateTransactionFees(homePrice: number, state: LaunchState): TransactionFees`
       - Reuse estimateTitleFee from Phase 1 calculator.ts
       - Platform fee: PLATFORM_FEE_PLACEHOLDER (from constants)
       - Attorney fee: $1,500 (150000 cents) for attorney-required and customary-attorney states, 0 otherwise
       - Agent-for-hire fee: 0 (placeholder for Phase 5 — add comment)
       - MLS syndication fee: $299 (29900 cents) flat fee — this is the flat-fee MLS broker partner fee
       - Total: sum of all fees
       - Traditional commission: homePrice * COMMISSION_RATE_DEFAULT
       - Savings: traditionalCommission - totalFees

    3. Create src/components/fees/FeeBreakdown.tsx ("use client"):
       - Accept homePrice (number, cents) and state (LaunchState) props
       - Call calculateTransactionFees
       - Display as a shadcn/ui Card with table layout:
         - Row: "Platform Fee" — formatted as $25.00
         - Row: "Title Company Fee" — formatted
         - Row: "Attorney Fee" — formatted (only show if > 0, with note "(required in {state})" or "(customary in {state})")
         - Row: "Flat-Fee MLS Listing" — formatted as $299
         - Row: "Agent-for-Hire Fee" — show "N/A" or "$0" with note "(if needed — see Phase 5)"
         - Separator line
         - Row: "Total Platform Fees" — bold, formatted
         - Row: "Traditional Agent Commission (5.5%)" — formatted, struck-through or red
         - Row: "Your Savings" — bold, green, formatted
       - All dollar amounts formatted from cents to dollars with $ prefix and commas
       - Small print at bottom: "Fees are estimates and may vary. Title company fees depend on your chosen provider."

    4. Create src/components/fees/FeeBreakdown.test.tsx:
       - Use Vitest + React Testing Library (or just test the component output logic)
       - Test that GA listing shows attorney fee row
       - Test that CA listing does not show attorney fee row
       - Test that savings calculation is correct
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/services/fees/transaction-fees.test.ts src/components/fees/FeeBreakdown.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - Transaction fee service calculates all fee components correctly
    - FeeBreakdown component renders state-aware fee table
    - Attorney fees shown only for applicable states
    - Savings comparison to traditional commission displayed
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: MLS syndication stub + listing page integration</name>
  <files>src/services/mls/syndication.ts, src/app/listings/[id]/page.tsx, src/app/seller/listings/[id]/syndicate/page.tsx</files>
  <action>
    1. Create src/services/mls/syndication.ts:
       - Export interface `MlsSyndicationRequest { listingId: string; sellerUserId: string; mlsRegion: string; }`
       - Export interface `MlsSyndicationStatus { status: "pending" | "submitted" | "active" | "rejected"; submittedAt?: Date; mlsNumber?: string; }`
       - Export `submitToMls(request: MlsSyndicationRequest): Promise<MlsSyndicationStatus>`
         - STUB: Returns { status: "pending" } immediately
         - Comment: `// TODO: Integrate with flat-fee MLS broker partner API/webhook when partnership is established. See 02-RESEARCH.md "MLS Syndication" section. Current options: ListWithFreedom, Homecoin, FlatFeeGroup — all require vendor negotiation.`
       - Export `getMlsStatus(listingId: string): Promise<MlsSyndicationStatus | null>`
         - STUB: Returns null (no MLS submission yet)

    2. Update src/app/listings/[id]/page.tsx:
       - Import FeeBreakdown component
       - Replace fee breakdown placeholder div with `<FeeBreakdown homePrice={listing.price} state={listing.state as LaunchState} />`
       - FeeBreakdown renders for all active/pending listings

    3. Create src/app/seller/listings/[id]/syndicate/page.tsx:
       - Server component: seller-only page for MLS syndication
       - Display: MLS syndication status, "Submit to MLS" button (calls submitToMls)
       - Show fee: $299 flat-fee MLS listing fee
       - Note: "MLS syndication is currently in beta. Your listing will be submitted to a partner broker for MLS listing."
       - This page is a future integration point — for now it shows the stub status
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - FeeBreakdown component rendered on listing detail pages
    - Fee breakdown visible before user commits (COST-04)
    - MLS syndication stub exists with clear integration notes
    - Seller MLS syndication page exists at /seller/listings/[id]/syndicate
    - Build compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/services/fees/ src/components/fees/ --reporter=verbose` — all fee tests pass
- `npx next build 2>&1 | tail -20` — build succeeds
- Listing detail page renders FeeBreakdown with correct state-aware fees
</verification>

<success_criteria>
- Full fee breakdown shows platform fee, title, attorney (where applicable), MLS fee (COST-03)
- Fee breakdown visible on listing pages before any commitment (COST-04)
- MLS syndication stub ready for broker partner integration
- Fee calculations are state-aware and use Phase 1 constants
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-07-SUMMARY.md`
</output>
