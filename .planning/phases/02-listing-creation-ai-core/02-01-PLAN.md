---
phase: 02-listing-creation-ai-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/types/index.ts
  - src/services/listing/create.ts
  - src/services/listing/create.test.ts
  - src/services/listing/photos.ts
  - src/services/listing/photos.test.ts
  - src/app/api/upload/presign/route.ts
  - src/app/api/listings/route.ts
  - src/app/api/listings/[id]/route.ts
  - src/inngest/client.ts
  - src/app/api/inngest/route.ts
  - .env.example
autonomous: true
requirements: [LIST-01, LIST-02, LIST-08]

must_haves:
  truths:
    - "Listing can be created with all required fields (address, price, beds, baths, sqft, lot size, property type)"
    - "Photos can be uploaded to R2 via presigned PUT URL and associated with a listing"
    - "Photo order is stored as text[] on listing and can be reordered"
    - "Land/lot listings allow null beds/baths while residential requires them"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "listings, listingPhotos, showingRequests, knowledgeChunks tables + property_type and listing_status enums"
      contains: "listings"
    - path: "src/services/listing/create.ts"
      provides: "createListing service function with Zod validation"
      exports: ["createListing"]
    - path: "src/services/listing/photos.ts"
      provides: "Photo ordering and R2 URL management"
      exports: ["reorderPhotos", "addPhotoToListing", "removePhotoFromListing"]
    - path: "src/app/api/upload/presign/route.ts"
      provides: "R2 presigned PUT URL generation"
      exports: ["POST"]
    - path: "src/app/api/listings/route.ts"
      provides: "Listing CRUD API"
      exports: ["POST", "GET"]
    - path: "src/inngest/client.ts"
      provides: "Inngest singleton client"
      exports: ["inngest"]
  key_links:
    - from: "src/services/listing/create.ts"
      to: "src/db/schema.ts"
      via: "Drizzle insert into listings table"
      pattern: "db\\.insert\\(listings\\)"
    - from: "src/app/api/upload/presign/route.ts"
      to: "R2"
      via: "S3Client presigned PUT URL"
      pattern: "getSignedUrl"
    - from: "src/app/api/listings/route.ts"
      to: "src/services/listing/create.ts"
      via: "createListing service call"
      pattern: "createListing"
---

<objective>
Create the listing data model (Drizzle schema), photo storage adapter (Cloudflare R2 presigned URLs), listing CRUD API routes, and Inngest client singleton. This is the data foundation that all other Phase 2 plans depend on.

Purpose: Every subsequent plan (UI, detail pages, AI, chatbot, fees) needs the listings/photos schema and API to exist first.
Output: Drizzle schema with listings + photos tables, listing service layer, R2 presigned upload route, listings API routes, Inngest client.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-listing-creation-ai-core/02-RESEARCH.md

<interfaces>
<!-- Existing types and schema the executor needs -->

From src/db/schema.ts:
```typescript
import { pgEnum, pgTable, text, timestamp } from "drizzle-orm/pg-core";
export const userRoleEnum = pgEnum("user_role", ["buyer", "seller"]);
export const users = pgTable("users", {
  id: text("id").primaryKey(),
  email: text("email").notNull().unique(),
  role: userRoleEnum("role").default("buyer").notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

From src/types/index.ts:
```typescript
export type UserRole = "buyer" | "seller";
export type LaunchState = (typeof LAUNCH_STATES)[number];
export type ClosingType = "title-company" | "attorney-required" | "customary-attorney";
export interface CommissionBreakdown { ... }
```

From src/lib/constants.ts:
```typescript
export const PLATFORM_FEE_PLACEHOLDER = 2500;
export const LAUNCH_STATES = ["CA","TX","FL","NY","GA","NC","AZ","OH","PA","IL"] as const;
export const ATTORNEY_STATES = ["GA", "NC"] as const;
export const CUSTOMARY_ATTORNEY_STATES = ["NY", "IL"] as const;
export const COMMISSION_RATE_DEFAULT = 0.055;
```

From src/db/index.ts (Phase 1):
```typescript
// Drizzle singleton with postgres() and prepare: false
// Import as: import { db } from "@/db";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Listing schema + service layer with tests</name>
  <files>src/db/schema.ts, src/types/index.ts, src/services/listing/create.ts, src/services/listing/create.test.ts, src/services/listing/photos.ts, src/services/listing/photos.test.ts</files>
  <behavior>
    - Test: createListing with valid residential data returns listing object with all fields
    - Test: createListing with land_lot property type allows null beds/baths
    - Test: createListing with residential type and missing beds rejects with validation error
    - Test: createListing with price below $10,000 (1000000 cents) rejects
    - Test: addPhotoToListing appends photo ID to photoOrder array
    - Test: reorderPhotos updates photoOrder array to new order
    - Test: removePhotoFromListing removes photo ID from photoOrder array
  </behavior>
  <action>
    1. Extend src/db/schema.ts with these additions (keep existing users table):
       - `propertyTypeEnum`: pgEnum with "single_family", "condo", "townhouse", "land_lot"
       - `listingStatusEnum`: pgEnum with "draft", "active", "pending", "sold"
       - `listings` table: id (text PK, use crypto.randomUUID), userId (text, NOT NULL), streetAddress, city, state, zip, propertyType, price (integer, in cents), bedrooms (integer, nullable), bathrooms (numeric 3,1, nullable), sqft (integer, nullable), lotSizeSqft (integer, nullable), yearBuilt (integer, nullable), description (text, nullable), descriptionStatus (text, default "pending"), status (listingStatusEnum default "draft"), photoOrder (text array default []), createdAt, updatedAt, publishedAt (nullable). Indexes on userId, status, state.
       - `listingPhotos` table: id (text PK), listingId (FK to listings, cascade delete), r2Key (text), r2Url (text), width (int nullable), height (int nullable), sizeBytes (int nullable), uploadedAt.
       - `showingRequests` table: id (text PK), listingId (FK to listings), buyerUserId (text nullable), requestedDate (timestamp), status (text default "pending"), notes (text nullable), createdAt.
       - `knowledgeChunks` table: id (text PK), content (text), source (text), state (text nullable), createdAt. Note: vector(1536) embedding column added via raw SQL migration, not Drizzle schema.
       - Set up Drizzle relations for listings -> listingPhotos (one-to-many).

    2. Add to src/types/index.ts:
       - `PropertyType` union: "single_family" | "condo" | "townhouse" | "land_lot"
       - `ListingStatus` union: "draft" | "active" | "pending" | "sold"
       - `ListingFormData` interface with all form fields
       - `Listing` type inferred from Drizzle schema (use `typeof listings.$inferSelect`)

    3. Create src/services/listing/create.ts:
       - Export `listingSchema` Zod validation: streetAddress min(5), city min(2), state length(2), zip regex 5-digit, propertyType enum, price int min(1000000) max(10000000000), bedrooms/bathrooms/sqft optional (use .refine() to require beds/baths when propertyType !== "land_lot"), lotSizeSqft optional
       - Export `createListing(userId: string, data: ListingFormData)`: validates with Zod, inserts via Drizzle, returns listing. Generates ID with crypto.randomUUID().

    4. Create src/services/listing/photos.ts:
       - Export `addPhotoToListing(listingId, photoId)`: appends to photoOrder array via SQL array_append
       - Export `removePhotoFromListing(listingId, photoId)`: removes from photoOrder via SQL array_remove, deletes photo row
       - Export `reorderPhotos(listingId, newOrder: string[])`: updates photoOrder to newOrder

    5. Write tests FIRST (TDD red-green):
       - src/services/listing/create.test.ts: Mock db. Test validation (valid residential, valid land, missing required fields, price bounds). Test that createListing calls db.insert with correct shape.
       - src/services/listing/photos.test.ts: Mock db. Test addPhotoToListing, removePhotoFromListing, reorderPhotos.

    Use Drizzle `relations()` to define the listings-to-photos one-to-many relationship for `.query.listings.findFirst({ with: { photos: true } })` support.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/services/listing/create.test.ts src/services/listing/photos.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - listings, listingPhotos, showingRequests, knowledgeChunks tables defined in schema.ts
    - PropertyType, ListingStatus, Listing types exported from types/index.ts
    - createListing service validates and inserts correctly; land/lot allows null beds/baths
    - Photo ordering functions (add, remove, reorder) work correctly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: API routes + R2 presigned upload + Inngest client</name>
  <files>src/app/api/upload/presign/route.ts, src/app/api/listings/route.ts, src/app/api/listings/[id]/route.ts, src/inngest/client.ts, src/app/api/inngest/route.ts, .env.example</files>
  <action>
    1. Create src/inngest/client.ts:
       - Export singleton Inngest client: `new Inngest({ id: "realestatehunter" })`

    2. Create src/app/api/inngest/route.ts:
       - Import `serve` from "inngest/next", import inngest client
       - Export `{ GET, POST, PUT }` from serve({ client: inngest, functions: [] })
       - Functions array will be populated in plan 02-04 when generate-description is built

    3. Create src/app/api/upload/presign/route.ts:
       - POST handler: auth() check (401 if no userId), parse { fileName, contentType, listingId } from body
       - Create S3Client pointing to R2 endpoint (env vars: R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY)
       - Generate key: `listings/${listingId}/${crypto.randomUUID()}-${fileName}`
       - getSignedUrl with PutObjectCommand, 300s expiry
       - Return { url, key }

    4. Create src/app/api/listings/route.ts:
       - POST: auth() check, parse body with listingSchema, call createListing(userId, data), fire inngest.send({ name: "listing/created", data: { listingId, ... } }) (fire-and-forget for now, handler added in 02-04), return 201 with listing
       - GET: auth() check, query listings WHERE userId = current user, return list

    5. Create src/app/api/listings/[id]/route.ts:
       - GET: fetch listing by ID with photos relation, return 404 if not found
       - PATCH: auth() check, verify ownership, update listing fields, revalidateTag("listings"), return updated
       - DELETE: auth() check, verify ownership, delete listing (cascades photos), return 204

    6. Update .env.example with new env vars:
       - R2_ENDPOINT, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME
       - R2_PUBLIC_URL (for constructing photo URLs)
       - OPENAI_API_KEY (for plans 02-04, 02-06)
       - INNGEST_EVENT_KEY, INNGEST_SIGNING_KEY (optional for dev)

    Install new dependencies: `npm install inngest ai @ai-sdk/openai react-dropzone @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - R2 presigned upload endpoint returns signed URL
    - Listings CRUD API supports POST (create), GET (list), GET by ID, PATCH (update), DELETE
    - Inngest client singleton created, serve route registered
    - All Phase 2 dependencies installed
    - Build passes with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/services/listing/ --reporter=verbose` — all listing service tests pass
- `npx next build 2>&1 | tail -20` — build succeeds with no errors
- Schema file exports listings, listingPhotos, showingRequests, knowledgeChunks tables
- API routes exist at /api/listings, /api/listings/[id], /api/upload/presign, /api/inngest
</verification>

<success_criteria>
- Listing data model is complete with all fields from LIST-01 requirements
- Photo storage adapter (R2 presigned URLs) is functional
- Land/lot property type support (LIST-08) works with nullable beds/baths
- Photo ordering (LIST-02 data layer) stores order as text[] on listing
- Inngest client singleton exists for async AI jobs
- All service-layer tests pass
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-01-SUMMARY.md`
</output>
