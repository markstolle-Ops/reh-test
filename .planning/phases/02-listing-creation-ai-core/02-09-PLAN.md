---
phase: 02-listing-creation-ai-core
plan: 09
type: execute
wave: 2
depends_on: ["02-08"]
files_modified:
  - src/app/api/upload/presign/route.ts
  - src/app/api/listings/[id]/route.ts
  - src/components/listing/PhotoUploader.tsx
autonomous: true
gap_closure: true
requirements:
  - LIST-02
  - LIST-03
  - LIST-08
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - COST-03
  - COST-04

must_haves:
  truths:
    - "PhotoUploader sends correct field names to presign route and parses the response correctly"
    - "PATCH /api/listings/[id] detects addPhoto/removePhoto/reorderPhotos and dispatches to photo service functions"
    - "After R2 upload, photo record is created in DB and returned to PhotoUploader"
    - "Photo delete removes DB record and updates photoOrder array"
    - "Photo reorder updates photoOrder array via PATCH"
  artifacts:
    - path: "src/app/api/upload/presign/route.ts"
      provides: "Presigned URL with field names matching PhotoUploader"
      contains: "uploadUrl"
    - path: "src/app/api/listings/[id]/route.ts"
      provides: "Photo operation dispatch in PATCH handler"
      contains: "addPhoto"
    - path: "src/components/listing/PhotoUploader.tsx"
      provides: "Correct presign request body with contentType and listingId"
      contains: "contentType"
  key_links:
    - from: "src/components/listing/PhotoUploader.tsx"
      to: "/api/upload/presign"
      via: "fetch POST with { fileName, contentType, listingId }"
      pattern: "contentType.*listingId"
    - from: "src/components/listing/PhotoUploader.tsx"
      to: "/api/listings/[id] PATCH"
      via: "fetch PATCH with { addPhoto: { r2Key, r2Url } }"
      pattern: "addPhoto"
    - from: "src/app/api/listings/[id]/route.ts"
      to: "src/services/listing/photos.ts"
      via: "addPhotoToListing, removePhotoFromListing, reorderPhotos"
      pattern: "addPhotoToListing|removePhotoFromListing|reorderPhotos"
---

<objective>
Fix the photo upload pipeline: presign contract mismatch and PATCH photo operation handling.

Purpose: Photo upload is completely non-functional because (a) PhotoUploader sends wrong field names to the presign route and expects wrong response fields, and (b) PATCH /api/listings/[id] blindly spreads addPhoto/removePhoto/reorderPhotos into the listings table instead of dispatching to photo service functions. This plan fixes both sides of the contract.

Output: Working photo upload flow — presign, R2 PUT, photo record creation, deletion, and reorder all functional.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-listing-creation-ai-core/02-VERIFICATION.md

<interfaces>
<!-- Photo service functions (already working, just need to be called from PATCH) -->

From src/services/listing/photos.ts:
```typescript
export async function addPhotoToListing(listingId: string, photoId: string): Promise<void>;
export async function removePhotoFromListing(listingId: string, photoId: string): Promise<void>;
export async function reorderPhotos(listingId: string, newOrder: string[]): Promise<void>;
```

From src/db/schema.ts (listingPhotos table):
```typescript
// listingPhotos has: id (uuid), listingId (uuid FK), r2Key (text), r2Url (text), createdAt
```

<!-- Current broken presign route response -->
From src/app/api/upload/presign/route.ts:
- Expects request body: { fileName, contentType, listingId }
- Returns: { url, key }

<!-- Current broken PhotoUploader expectations -->
From src/components/listing/PhotoUploader.tsx:
- Sends to presign: { fileName, fileType, fileSize } -- wrong (should be contentType, missing listingId)
- Expects from presign: { uploadUrl, key, publicUrl } -- wrong (route returns { url, key })
- Sends to PATCH: { addPhoto: { r2Key, r2Url } } -- PATCH doesn't handle this
- Expects from PATCH addPhoto: { photo } response
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix presign route response and PhotoUploader request</name>
  <files>src/app/api/upload/presign/route.ts, src/components/listing/PhotoUploader.tsx</files>
  <action>
Align the presign contract by updating BOTH sides to a clean consistent contract:

1. In `src/app/api/upload/presign/route.ts` (line 52):
   - Change `return NextResponse.json({ url, key })` to `return NextResponse.json({ uploadUrl: url, key, publicUrl: \`\${process.env.NEXT_PUBLIC_R2_PUBLIC_URL}/\${key}\` })`
   - This returns `{ uploadUrl, key, publicUrl }` which is what PhotoUploader expects
   - The publicUrl is constructed from the R2 public URL base + key (per existing decision: NEXT_PUBLIC_R2_PUBLIC_URL required for client components)

2. In `src/components/listing/PhotoUploader.tsx` (lines 118-122):
   - Change the presign request body from `{ fileName: file.name, fileType: file.type, fileSize: file.size }` to `{ fileName: file.name, contentType: file.type, listingId }`
   - This matches what the presign route expects: `{ fileName, contentType, listingId }`
   - The `listingId` is already available as a prop on PhotoUploader

The response destructuring on line 129 (`const { uploadUrl, key, publicUrl } = await presignRes.json()`) is already correct after the presign route fix.

The R2 PUT on line 139 (`await fetch(uploadUrl, { method: "PUT", ... })`) will now work because `uploadUrl` is the actual presigned URL.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build --webpack 2>&1 | tail -5</automated>
  </verify>
  <done>
- Presign route returns { uploadUrl, key, publicUrl }
- PhotoUploader sends { fileName, contentType, listingId } to presign
- PhotoUploader correctly uses uploadUrl for R2 PUT and publicUrl for photo record
  </done>
</task>

<task type="auto">
  <name>Task 2: Add photo operation dispatch to PATCH handler</name>
  <files>src/app/api/listings/[id]/route.ts</files>
  <action>
Rewrite the PATCH handler in `src/app/api/listings/[id]/route.ts` to detect and dispatch photo operations instead of blindly spreading the body into `db.update(listings)`.

The PATCH handler must:

1. Import photo service functions at the top of the file:
   ```
   import { addPhotoToListing, removePhotoFromListing, reorderPhotos } from "@/services/listing/photos";
   import { db as database } from "@/db";
   import { listingPhotos } from "@/db/schema";
   ```

2. After auth check and ownership verification (keep existing lines 36-55), parse the body and check for photo operation keys:

   a. If `body.addPhoto` exists (shape: `{ r2Key: string, r2Url: string }`):
      - Insert a new row into `listingPhotos` table: `{ id: crypto.randomUUID(), listingId: id, r2Key: body.addPhoto.r2Key, r2Url: body.addPhoto.r2Url }`
      - Call `addPhotoToListing(id, newPhotoId)` to append to photoOrder
      - Return `NextResponse.json({ photo: { id: newPhotoId, r2Key: body.addPhoto.r2Key, r2Url: body.addPhoto.r2Url } })`

   b. If `body.removePhoto` exists (shape: string photoId):
      - Call `removePhotoFromListing(id, body.removePhoto)`
      - Return `NextResponse.json({ success: true })`

   c. If `body.reorderPhotos` exists (shape: string[] of photo IDs):
      - Call `reorderPhotos(id, body.reorderPhotos)`
      - Return `NextResponse.json({ success: true })`

   d. Otherwise (no photo operation keys): This is a normal field update.
      - Keep existing behavior: spread body into `db.update(listings).set(...)` but ONLY allow known listing fields (strip unknown keys to prevent column injection)
      - Allowed fields: streetAddress, city, state, zip, propertyType, price, bedrooms, bathrooms, sqft, lotSizeSqft, yearBuilt, description, descriptionStatus, status
      - Return `NextResponse.json({ listing: updated })` (wrapped — consistent with Plan 08)

3. Keep `revalidateTag("listings")` call (single arg, fixed in Plan 08) after any mutation.

IMPORTANT: The `{ listing: updated }` wrapper for normal PATCH responses was already handled in Plan 08. If Plan 08 has already executed when this runs, do not double-wrap. The key point is: photo operations return `{ photo }` or `{ success }`, normal updates return `{ listing }`.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build --webpack 2>&1 | tail -5</automated>
  </verify>
  <done>
- PATCH with { addPhoto: { r2Key, r2Url } } creates photo record in DB, appends to photoOrder, returns { photo }
- PATCH with { removePhoto: photoId } deletes photo record, removes from photoOrder, returns { success }
- PATCH with { reorderPhotos: [...ids] } updates photoOrder array, returns { success }
- PATCH with normal listing fields updates only allowed columns, returns { listing }
- No unknown keys can be spread into the listings table
  </done>
</task>

</tasks>

<verification>
After both tasks, the following truths from VERIFICATION.md should now pass:
- "Photos can be uploaded to R2 via presigned PUT URL and associated with a listing" (was FAILED)
- "Seller can upload photos with drag-and-drop" (was FAILED — depended on presign + PATCH)
- "Photos can be added to a listing via PATCH" (was FAILED)
</verification>

<success_criteria>
- `next build --webpack` succeeds
- All 3 photo-related gaps from VERIFICATION.md are resolved
- Photo upload pipeline: presign -> R2 PUT -> PATCH addPhoto -> photo record in DB
- Photo delete: PATCH removePhoto -> photo record deleted, photoOrder updated
- Photo reorder: PATCH reorderPhotos -> photoOrder updated
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-09-SUMMARY.md`
</output>
