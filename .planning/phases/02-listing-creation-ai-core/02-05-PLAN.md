---
phase: 02-listing-creation-ai-core
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - src/services/neighborhood/data.ts
  - src/services/neighborhood/data.test.ts
  - src/components/neighborhood/NeighborhoodWidget.tsx
  - src/components/neighborhood/MarketTrends.tsx
  - src/app/api/neighborhood/route.ts
  - src/app/listings/[id]/page.tsx
autonomous: true
requirements: [DATA-03, DATA-04]

must_haves:
  truths:
    - "Buyer can view neighborhood data (walkability, school ratings) on listing pages"
    - "Market analysis shows price trends, days on market, and inventory levels per area"
    - "Neighborhood widget displays data with source attribution"
    - "Both widgets are rendered on the listing detail page"
  artifacts:
    - path: "src/services/neighborhood/data.ts"
      provides: "Neighborhood data service (stub adapter)"
      exports: ["getNeighborhoodData", "getMarketTrends"]
    - path: "src/components/neighborhood/NeighborhoodWidget.tsx"
      provides: "Walkability, school ratings, neighborhood info display"
    - path: "src/components/neighborhood/MarketTrends.tsx"
      provides: "Recharts market trends chart (price, DOM, inventory)"
  key_links:
    - from: "src/app/api/neighborhood/route.ts"
      to: "src/services/neighborhood/data.ts"
      via: "getNeighborhoodData + getMarketTrends calls"
      pattern: "getNeighborhoodData|getMarketTrends"
    - from: "src/components/neighborhood/MarketTrends.tsx"
      to: "recharts"
      via: "Recharts LineChart for trend visualization"
      pattern: "LineChart|ResponsiveContainer"
    - from: "src/app/listings/[id]/page.tsx"
      to: "src/components/neighborhood/NeighborhoodWidget.tsx"
      via: "NeighborhoodWidget rendered on listing detail page"
      pattern: "NeighborhoodWidget"
    - from: "src/app/listings/[id]/page.tsx"
      to: "src/components/neighborhood/MarketTrends.tsx"
      via: "MarketTrends rendered on listing detail page"
      pattern: "MarketTrends"
---

<objective>
Build neighborhood data and market trends widgets for listing pages, and wire them into the listing detail page. Uses stub adapters ready for Walk Score / GreatSchools / HouseCanary API integration when vendor contracts are signed.

Purpose: DATA-03 and DATA-04 give buyers the market context they need to make informed decisions without requiring a real estate agent's local knowledge.
Output: Neighborhood data service, neighborhood widget, market trends chart, API route, all integrated into listing detail page.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-listing-creation-ai-core/02-RESEARCH.md
@.planning/phases/02-listing-creation-ai-core/02-01-SUMMARY.md
@.planning/phases/02-listing-creation-ai-core/02-03-SUMMARY.md

<interfaces>
From src/app/listings/[id]/page.tsx (02-03):
```typescript
// Listing detail page — has placeholder divs for neighborhood and market trends widgets
// Server component with ISR caching
// listing object has: zip, state, streetAddress, city, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Neighborhood data + market trends service with tests</name>
  <files>src/services/neighborhood/data.ts, src/services/neighborhood/data.test.ts, src/app/api/neighborhood/route.ts</files>
  <behavior>
    - Test: getNeighborhoodData returns { walkScore, transitScore, bikeScore, schoolRating, crimeIndex } for a valid zip
    - Test: getNeighborhoodData returns null for invalid zip
    - Test: getMarketTrends returns array of { month, medianPrice, daysOnMarket, activeInventory } for a valid zip
    - Test: getMarketTrends returns 12 months of data
  </behavior>
  <action>
    1. Write tests FIRST in src/services/neighborhood/data.test.ts.

    2. Create src/services/neighborhood/data.ts:
       - Export interface `NeighborhoodData { walkScore: number; transitScore: number; bikeScore: number; schoolRating: number; nearbySchools: { name: string; rating: number; distance: string }[]; crimeIndex: number; }`
       - Export interface `MarketTrendPoint { month: string; medianPrice: number; daysOnMarket: number; activeInventory: number; }`
       - Export `getNeighborhoodData(zip: string, state: string): Promise<NeighborhoodData | null>`
         - STUB: Generate deterministic data from zip hash. Different zips produce different scores. Set realistic ranges (walk 0-100, schools 1-10, etc.)
         - Comment: `// TODO: Replace with Walk Score API + GreatSchools API when vendor contracts signed`
       - Export `getMarketTrends(zip: string, state: string): Promise<MarketTrendPoint[]>`
         - STUB: Generate 12 months of trend data with realistic seasonal patterns. Use zip-based seed for deterministic output.
         - Comment: `// TODO: Replace with HouseCanary market analytics or ATTOM market stats endpoint`

    3. Create src/app/api/neighborhood/route.ts:
       - GET handler: parse query params (zip, state)
       - No auth required (public data for listing pages)
       - Return { neighborhood: NeighborhoodData, trends: MarketTrendPoint[] }
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/services/neighborhood/data.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Neighborhood data service returns realistic stub data
    - Market trends service returns 12 months of trend data
    - API route serves both at GET /api/neighborhood
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Neighborhood widget + market trends chart + listing page wiring</name>
  <files>src/components/neighborhood/NeighborhoodWidget.tsx, src/components/neighborhood/MarketTrends.tsx, src/app/listings/[id]/page.tsx</files>
  <action>
    1. Create src/components/neighborhood/NeighborhoodWidget.tsx ("use client"):
       - Accept zip and state props
       - Fetch from /api/neighborhood on mount
       - Display: Walk Score (0-100 with color indicator), Transit Score, Bike Score
       - Display: School Rating (1-10 with color), list of up to 3 nearby schools with name, rating, distance
       - Display: Crime Index with relative indicator (low/medium/high)
       - Loading skeleton while fetching
       - Source attribution: "Data sources: Walk Score, GreatSchools (stub data — real APIs pending)"
       - Use shadcn/ui Card for each section

    2. Create src/components/neighborhood/MarketTrends.tsx ("use client"):
       - Accept zip and state props
       - Fetch trends from /api/neighborhood on mount (reuse same endpoint)
       - Recharts LineChart (inside ResponsiveContainer) showing:
         - Line 1: Median Price over 12 months (left Y axis, formatted as $XXXk)
         - Line 2: Days on Market over 12 months (right Y axis)
       - Below chart: summary stats cards — current median price, average DOM, active inventory count
       - Use "use client" directive (Recharts requires window)
       - Source attribution: "Market data powered by RealEstateHunter Analytics (stub data — real APIs pending)"

    3. Update src/app/listings/[id]/page.tsx:
       - Import NeighborhoodWidget from "@/components/neighborhood/NeighborhoodWidget"
       - Import MarketTrends from "@/components/neighborhood/MarketTrends"
       - Replace the neighborhood placeholder div with `<NeighborhoodWidget zip={listing.zip} state={listing.state} />`
       - Replace the market trends placeholder div with `<MarketTrends zip={listing.zip} state={listing.state} />`
       - Render both for active/pending listings (not draft/sold)
       - This enables DATA-03 (neighborhood data on listing pages) and DATA-04 (market trends visible to buyers)
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - NeighborhoodWidget renders walk/transit/bike scores, school ratings, crime index
    - MarketTrends renders Recharts LineChart with 12-month price and DOM trends
    - Both components handle loading and error states
    - Source attributions displayed
    - Both components are wired into listing detail page (src/app/listings/[id]/page.tsx)
    - Build compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/services/neighborhood/data.test.ts --reporter=verbose` — all tests pass
- `npx next build 2>&1 | tail -20` — build succeeds
</verification>

<success_criteria>
- Neighborhood data displays on listing pages (DATA-03)
- Market trends chart shows price trends, DOM, inventory (DATA-04)
- Both widgets wired into listing detail page
- Stub adapters clearly marked for real API replacement
- Components use Recharts for visualization (consistent with Phase 1 calculator charts)
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-05-SUMMARY.md`
</output>
