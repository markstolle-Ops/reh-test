---
phase: 02-listing-creation-ai-core
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/listings/[id]/page.tsx
  - src/components/listing/ListingGallery.tsx
  - src/components/listing/ListingDetails.tsx
  - src/components/listing/ListingHeader.tsx
autonomous: true
requirements: [LIST-07]

must_haves:
  truths:
    - "Public listing detail page renders at /listings/[id] with full property details"
    - "Photo gallery displays all listing photos with navigation"
    - "Listing page uses ISR with tag-based revalidation"
    - "Non-existent listing ID returns 404"
  artifacts:
    - path: "src/app/listings/[id]/page.tsx"
      provides: "ISR listing detail page with unstable_cache + revalidateTag"
      min_lines: 40
    - path: "src/components/listing/ListingGallery.tsx"
      provides: "Photo gallery with thumbnails and full-size view"
      min_lines: 30
    - path: "src/components/listing/ListingDetails.tsx"
      provides: "Property details display (beds, baths, sqft, price, etc.)"
  key_links:
    - from: "src/app/listings/[id]/page.tsx"
      to: "src/db/schema.ts"
      via: "Drizzle query with unstable_cache"
      pattern: "unstable_cache"
    - from: "src/app/listings/[id]/page.tsx"
      to: "src/components/listing/ListingGallery.tsx"
      via: "Component import"
      pattern: "ListingGallery"
---

<objective>
Build public listing detail pages with photo gallery, full property details, and ISR caching. These pages are the buyer-facing view of listings.

Purpose: LIST-07 requires listings to display on the platform with photo gallery and full details. This page is also the container for the chatbot (02-06) and fee breakdown (02-07).
Output: Public listing detail page at /listings/[id] with photo gallery, property details, and ISR.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-listing-creation-ai-core/02-RESEARCH.md
@.planning/phases/02-listing-creation-ai-core/02-01-SUMMARY.md

<interfaces>
<!-- From plan 02-01 -->
From src/db/schema.ts:
```typescript
export const listings = pgTable("listings", { ... });
export const listingPhotos = pgTable("listing_photos", { ... });
// Relations defined for with: { photos: true } query support
```

From src/types/index.ts:
```typescript
export type Listing = typeof listings.$inferSelect;
export type PropertyType = "single_family" | "condo" | "townhouse" | "land_lot";
export type ListingStatus = "draft" | "active" | "pending" | "sold";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Listing detail page with ISR + photo gallery</name>
  <files>src/app/listings/[id]/page.tsx, src/components/listing/ListingGallery.tsx, src/components/listing/ListingDetails.tsx, src/components/listing/ListingHeader.tsx</files>
  <action>
    1. Create src/components/listing/ListingGallery.tsx ("use client"):
       - Accept photos array prop (from listingPhotos relation)
       - Display main photo (large) with thumbnail strip below
       - Click thumbnail to switch main photo
       - Left/right arrow navigation
       - Keyboard arrow key support
       - If no photos, show placeholder "No photos available"
       - Use Cloudflare Images transform URL pattern: construct URLs from r2Key with size variants (listing-gallery for main, listing-thumb for thumbnails). If R2_PUBLIC_URL env var is not set, fall back to r2Url directly.

    2. Create src/components/listing/ListingHeader.tsx:
       - Display: price (formatted as $XXX,XXX), status badge, property type label
       - Address: street, city, state, zip
       - Key stats row: beds, baths, sqft, lot size (hide beds/baths for land_lot)

    3. Create src/components/listing/ListingDetails.tsx:
       - Full description text (the AI-generated or seller-edited description)
       - Year built, lot size details
       - Property type-specific sections
       - If description is null/pending, show "Description coming soon..."

    4. Create src/app/listings/[id]/page.tsx (Server Component):
       - Use unstable_cache to fetch listing by ID with photos relation
       - Cache key: ["listing-detail", id], tags: [`listing-${id}`, "listings"], revalidate: 3600
       - If listing not found or status === "draft", call notFound()
       - Layout: ListingGallery at top, ListingHeader below, ListingDetails below that
       - Leave placeholder slots for ChatWidget (02-06), FeeBreakdown (02-07), NeighborhoodWidget (02-05), AvmWidget (02-04) — render empty divs with id attributes so later plans can add components
       - Export generateMetadata for SEO: title = address, description = first 160 chars of description
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /listings/[id] renders listing with photo gallery, header, and details
    - Gallery navigates between photos via thumbnails and arrows
    - Draft listings return 404
    - ISR caching with tag-based revalidation is configured
    - SEO metadata exported
    - Placeholder slots exist for chatbot, fee breakdown, neighborhood, and AVM widgets
    - Build compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `npx next build 2>&1 | tail -20` — build succeeds, /listings/[id] route appears in output
- Listing detail page components render without TypeScript errors
</verification>

<success_criteria>
- Public listing detail page displays photo gallery and full property details (LIST-07)
- ISR caching with revalidateTag works
- Land/lot listings show appropriate details (no beds/baths)
- Page has placeholder slots for chatbot, fees, neighborhood data, AVM
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-03-SUMMARY.md`
</output>
