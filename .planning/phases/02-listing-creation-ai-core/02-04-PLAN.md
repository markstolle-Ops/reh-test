---
phase: 02-listing-creation-ai-core
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - src/inngest/functions/generate-description.ts
  - src/inngest/functions/generate-description.test.ts
  - src/ai/prompts/listing-description.ts
  - src/app/api/inngest/route.ts
  - src/services/avm/housecanary.ts
  - src/services/avm/housecanary.test.ts
  - src/components/avm/AvmWidget.tsx
  - src/app/api/avm/route.ts
  - src/app/listings/[id]/page.tsx
autonomous: true
requirements: [LIST-03, DATA-01, DATA-02]

must_haves:
  truths:
    - "AI generates MLS-quality listing description from property details and photos via Inngest async job"
    - "Listing creation triggers description generation; listing shows 'generating' status until ready"
    - "AVM endpoint returns home value estimate with confidence range for a given address"
    - "AVM widget displays estimate with range, provider attribution, and disclaimer"
    - "AVM widget is rendered on the listing detail page so sellers can see value before listing"
  artifacts:
    - path: "src/inngest/functions/generate-description.ts"
      provides: "Inngest function for GPT-4o vision description generation"
      exports: ["generateListingDescription"]
    - path: "src/ai/prompts/listing-description.ts"
      provides: "Versioned system prompt for MLS-quality descriptions"
      exports: ["LISTING_DESCRIPTION_PROMPT"]
    - path: "src/services/avm/housecanary.ts"
      provides: "AVM adapter (stub with mock data, ready for HouseCanary API swap)"
      exports: ["getHomeValueEstimate"]
    - path: "src/components/avm/AvmWidget.tsx"
      provides: "Home value display with range, attribution, and disclaimer"
  key_links:
    - from: "src/inngest/functions/generate-description.ts"
      to: "src/ai/prompts/listing-description.ts"
      via: "LISTING_DESCRIPTION_PROMPT import"
      pattern: "LISTING_DESCRIPTION_PROMPT"
    - from: "src/inngest/functions/generate-description.ts"
      to: "src/db/schema.ts"
      via: "Drizzle update to set description and descriptionStatus"
      pattern: "db\\.update\\(listings\\)"
    - from: "src/app/api/avm/route.ts"
      to: "src/services/avm/housecanary.ts"
      via: "getHomeValueEstimate call"
      pattern: "getHomeValueEstimate"
    - from: "src/app/listings/[id]/page.tsx"
      to: "src/components/avm/AvmWidget.tsx"
      via: "AvmWidget rendered in listing detail page AVM placeholder"
      pattern: "AvmWidget"
---

<objective>
Build the AI listing description generator (Inngest async job with GPT-4o vision) and the AVM (Automated Valuation Model) integration with a stub adapter ready for HouseCanary swap. Wire the AVM widget into the listing detail page.

Purpose: LIST-03 requires AI-generated MLS-quality descriptions. DATA-01/DATA-02 require home value estimates. Both are core AI differentiators.
Output: Inngest description generation function, AVM stub service, AVM API route, AVM widget component integrated into listing detail page.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-listing-creation-ai-core/02-RESEARCH.md
@.planning/phases/02-listing-creation-ai-core/02-01-SUMMARY.md
@.planning/phases/02-listing-creation-ai-core/02-03-SUMMARY.md

<interfaces>
From src/inngest/client.ts (02-01):
```typescript
export const inngest: Inngest;
```

From src/db/schema.ts (02-01):
```typescript
export const listings = pgTable("listings", {
  // ... includes description, descriptionStatus fields
});
```

From src/app/listings/[id]/page.tsx (02-03):
```typescript
// Listing detail page — has placeholder div with id for AVM widget
// Server component with ISR caching
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI description generator (Inngest + GPT-4o vision) with tests</name>
  <files>src/inngest/functions/generate-description.ts, src/inngest/functions/generate-description.test.ts, src/ai/prompts/listing-description.ts, src/app/api/inngest/route.ts</files>
  <behavior>
    - Test: generateListingDescription sets descriptionStatus to "generating" at start
    - Test: generateListingDescription calls AI SDK generateText with GPT-4o model and photo URLs as image content
    - Test: generateListingDescription saves generated text to description field and sets descriptionStatus to "ready"
    - Test: LISTING_DESCRIPTION_PROMPT includes property details (beds, baths, sqft, city, state) in output
    - Test: LISTING_DESCRIPTION_PROMPT instructs MLS-quality formatting (professional tone, highlight features)
  </behavior>
  <action>
    1. Write tests FIRST in src/inngest/functions/generate-description.test.ts:
       - Mock inngest step.run to execute the callback immediately
       - Mock ai generateText to return a fixed description string
       - Mock db.update to verify it's called with correct description and status
       - Test the full function flow: mark generating -> generate text -> save description

    2. Create src/ai/prompts/listing-description.ts:
       - Export `LISTING_DESCRIPTION_PROMPT(details: { beds, baths, sqft, lotSizeSqft, city, state, propertyType, yearBuilt })`: returns a string prompt
       - Prompt instructs GPT-4o to: write an MLS-quality listing description, highlight key features from the photos, mention the neighborhood/location, use professional real estate tone, keep to 200-400 words, do NOT mention price (seller may change it)
       - Include specific instruction: "Analyze the photos to identify and describe: kitchen upgrades, flooring, natural light, yard features, architectural style, and any visible amenities."

    3. Create src/inngest/functions/generate-description.ts:
       - Import inngest client, openai provider, generateText from ai, db, listings schema, LISTING_DESCRIPTION_PROMPT
       - `generateListingDescription` Inngest function:
         - Trigger: `event: "listing/created"`
         - Event data: `{ listingId, photoUrls, details }`
         - Step 1 ("mark-generating"): update descriptionStatus to "generating"
         - Step 2 ("generate-text"): call generateText with openai("gpt-4o"), messages with text prompt + up to 5 photo image URLs, maxTokens 800
         - Step 3 ("save-description"): update description and descriptionStatus to "ready"
         - Return { listingId, description }

    4. Update src/app/api/inngest/route.ts:
       - Add generateListingDescription to the functions array in serve()
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/inngest/functions/generate-description.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Inngest function generates descriptions via GPT-4o vision
    - Description status lifecycle: pending -> generating -> ready
    - Prompt produces MLS-quality output instructions
    - Tests pass with mocked AI SDK and DB
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AVM stub adapter + widget + API route + listing page wiring</name>
  <files>src/services/avm/housecanary.ts, src/services/avm/housecanary.test.ts, src/components/avm/AvmWidget.tsx, src/app/api/avm/route.ts, src/app/listings/[id]/page.tsx</files>
  <behavior>
    - Test: getHomeValueEstimate returns { estimatedValue, lowRange, highRange, confidence, provider } for a valid address
    - Test: getHomeValueEstimate returns null for an unknown/invalid address
    - Test: AvmWidget displays estimated value formatted as currency
    - Test: AvmWidget displays confidence range (low-high)
    - Test: AvmWidget includes "not an appraisal" disclaimer text
  </behavior>
  <action>
    1. Write tests FIRST in src/services/avm/housecanary.test.ts:
       - Test getHomeValueEstimate with mock address returns expected shape
       - Test invalid address returns null

    2. Create src/services/avm/housecanary.ts:
       - Export interface `AvmEstimate { estimatedValue: number; lowRange: number; highRange: number; confidence: number; provider: string; }`
       - Export `getHomeValueEstimate(address: { street: string; city: string; state: string; zip: string }): Promise<AvmEstimate | null>`
       - STUB implementation: Return a deterministic mock estimate based on state + zip (use a simple hash to generate realistic prices). Set provider to "RealEstateHunter Estimate (Stub)". When HouseCanary contract is signed, swap this function body to call the real API.
       - Add a comment: `// TODO: Replace stub with HouseCanary API call when vendor contract is signed. See 02-RESEARCH.md "AVM Provider" section.`

    3. Create src/app/api/avm/route.ts:
       - GET handler: parse query params (street, city, state, zip)
       - Auth check (Clerk auth())
       - Call getHomeValueEstimate
       - Return JSON response (or 404 if null)

    4. Create src/components/avm/AvmWidget.tsx ("use client"):
       - Accept address prop, fetch AVM estimate from /api/avm on mount
       - Display: "Estimated Value: $XXX,XXX" with formatted currency
       - Display: "Range: $XXX,XXX - $XXX,XXX" (lowRange - highRange)
       - Display: confidence score as percentage
       - Display: provider attribution ("Powered by {provider}")
       - Display: mandatory disclaimer: "This is an automated estimate, not an appraisal. For a precise value, consult a licensed appraiser."
       - Loading state while fetching
       - Error state if fetch fails

    5. Update src/app/listings/[id]/page.tsx:
       - Import AvmWidget from "@/components/avm/AvmWidget"
       - Replace the AVM placeholder div with `<AvmWidget address={{ street: listing.streetAddress, city: listing.city, state: listing.state, zip: listing.zip }} />`
       - Render AvmWidget for active/pending listings only (not draft/sold)
       - This enables DATA-01: seller can see AVM estimate on the listing page before committing to a price
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/services/avm/housecanary.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - AVM stub returns realistic estimate for any valid address
    - AVM API route serves estimate via GET /api/avm
    - AvmWidget displays value, range, confidence, attribution, and disclaimer
    - AvmWidget is rendered on listing detail page (src/app/listings/[id]/page.tsx)
    - Tests pass
    - Stub is clearly marked for replacement with real HouseCanary API
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/inngest/functions/generate-description.test.ts src/services/avm/housecanary.test.ts --reporter=verbose` — all tests pass
- `npx next build 2>&1 | tail -20` — build succeeds with new routes
</verification>

<success_criteria>
- AI description generation runs asynchronously via Inngest (LIST-03)
- AVM returns home value estimate with confidence range (DATA-01)
- AVM adapter is a stub ready for HouseCanary swap (DATA-02)
- AVM widget shows disclaimer and attribution
- AVM widget is wired into listing detail page
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-04-SUMMARY.md`
</output>
