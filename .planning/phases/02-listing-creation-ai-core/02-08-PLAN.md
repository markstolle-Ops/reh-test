---
phase: 02-listing-creation-ai-core
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/listings/route.ts
  - src/app/api/listings/[id]/route.ts
  - src/components/listing/ListingForm.tsx
  - src/app/seller/listings/[id]/edit/page.tsx
  - src/app/seller/listings/page.tsx
autonomous: true
gap_closure: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07

must_haves:
  truths:
    - "POST /api/listings returns { listing: ... } wrapper and callers parse it correctly"
    - "GET /api/listings/[id] returns { listing: ... } wrapper and callers parse it correctly"
    - "GET /api/listings returns { listings: [...] } wrapper and callers parse it correctly"
    - "Seller can see their listings on the seller dashboard (not empty)"
    - "Seller can edit listing details after publishing without runtime crash"
    - "AI description polling receives listing data and updates descriptionStatus"
  artifacts:
    - path: "src/app/api/listings/route.ts"
      provides: "Wrapped JSON responses for POST and GET"
      contains: "{ listing:"
    - path: "src/app/api/listings/[id]/route.ts"
      provides: "Wrapped JSON response for GET single listing"
      contains: "{ listing:"
  key_links:
    - from: "src/components/listing/ListingForm.tsx"
      to: "/api/listings"
      via: "fetch POST then destructure { listing }"
      pattern: "const \\{ listing \\} = await res\\.json"
    - from: "src/app/seller/listings/page.tsx"
      to: "/api/listings"
      via: "fetch GET then destructure { listings }"
      pattern: "data\\.listings"
    - from: "src/app/seller/listings/[id]/edit/page.tsx"
      to: "/api/listings/[id]"
      via: "fetch GET then destructure { listing }"
      pattern: "const \\{ listing \\} = await res\\.json"
---

<objective>
Fix API response shape mismatches between listing API routes and their callers.

Purpose: Five of the six verification gaps share a single root cause — API routes return bare objects/arrays but all callers destructure wrapped responses (`{ listing }` or `{ listings }`). This plan standardizes API responses to match caller expectations, unblocking listing creation, editing, dashboard display, and AI description polling.

Output: All three listing API response handlers return consistently wrapped JSON; all consumer components/pages parse correctly.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-listing-creation-ai-core/02-VERIFICATION.md

<interfaces>
<!-- Current broken API responses (to be fixed) -->

From src/app/api/listings/route.ts:
- Line 43: `return NextResponse.json(listing, { status: 201 });` -- bare object, callers expect `{ listing: ... }`
- Line 63: `return NextResponse.json(userListings);` -- bare array, callers expect `{ listings: [...] }`

From src/app/api/listings/[id]/route.ts:
- Line 27: `return NextResponse.json(listing);` -- bare object, callers expect `{ listing: ... }`

<!-- Current callers (these expect wrapped responses -- do NOT change these) -->

From src/components/listing/ListingForm.tsx:
- Line 102: `const { listing } = await res.json();` -- polling
- Line 172: `const { listing } = await res.json();` -- after create/edit submit

From src/app/seller/listings/[id]/edit/page.tsx:
- Line 46: `const { listing } = await res.json();`

From src/app/seller/listings/page.tsx:
- Line 47: `listings = data.listings ?? [];`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap listing API route responses</name>
  <files>src/app/api/listings/route.ts, src/app/api/listings/[id]/route.ts</files>
  <action>
Fix the three API response shapes to match what callers already expect:

1. In `src/app/api/listings/route.ts`:
   - Line 43: Change `NextResponse.json(listing, { status: 201 })` to `NextResponse.json({ listing }, { status: 201 })`
   - Line 63: Change `NextResponse.json(userListings)` to `NextResponse.json({ listings: userListings })`

2. In `src/app/api/listings/[id]/route.ts`:
   - Line 27: Change `NextResponse.json(listing)` to `NextResponse.json({ listing })`
   - Line 69: Change `NextResponse.json(updated)` to `NextResponse.json({ listing: updated })` (PATCH response — ListingForm also parses this after edit submit)

3. Also fix the `revalidateTag` call on line 67 of `[id]/route.ts`: `revalidateTag("listings", "default")` should be `revalidateTag("listings")` — revalidateTag only accepts one tag per call in Next.js 16, the second arg is silently ignored but is incorrect usage.

Do NOT modify any caller files (ListingForm, EditListingPage, SellerListingsPage) — their destructuring is already correct for the wrapped format.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build --webpack 2>&1 | tail -5</automated>
  </verify>
  <done>
- POST /api/listings returns `{ listing: { id, ... } }` with status 201
- GET /api/listings returns `{ listings: [...] }`
- GET /api/listings/[id] returns `{ listing: { id, ... } }`
- PATCH /api/listings/[id] returns `{ listing: { id, ... } }`
- revalidateTag called with single argument
- All consumer pages/components parse responses without error
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all callers parse correctly after response fix</name>
  <files>src/components/listing/ListingForm.tsx, src/app/seller/listings/[id]/edit/page.tsx, src/app/seller/listings/page.tsx</files>
  <action>
Read each caller file and confirm the destructuring matches the new wrapped response format. Specifically verify:

1. `ListingForm.tsx` line 172: `const { listing } = await res.json()` -- matches `{ listing: ... }` from POST and PATCH. Confirm `listing.id`, `listing.descriptionStatus` access is valid.

2. `ListingForm.tsx` line 102: `const { listing } = await res.json()` -- matches `{ listing: ... }` from GET /api/listings/[id]. Confirm `listing.descriptionStatus` and `listing.description` access is valid.

3. `EditListingPage` line 46: `const { listing } = await res.json()` -- matches `{ listing: ... }` from GET. Confirm `listing.userId` access on line 49 is valid.

4. `SellerListingsPage` line 47: `listings = data.listings ?? []` -- matches `{ listings: [...] }` from GET. Confirm iteration works.

If any caller has additional mismatches beyond the response wrapper (unlikely based on verification report), fix them. Otherwise, these files need zero changes — just confirm and document.
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx next build --webpack 2>&1 | tail -5</automated>
  </verify>
  <done>
- ListingForm create flow: POST response parsed, listing.id set, polling starts
- ListingForm edit flow: PATCH response parsed, redirect works
- ListingForm polling: GET response parsed, descriptionStatus updates
- EditListingPage: GET response parsed, listing.userId compared, form renders
- SellerListingsPage: GET response parsed, listings array iterated, cards render
  </done>
</task>

</tasks>

<verification>
After both tasks, the following truths from VERIFICATION.md should now pass:
- "Seller can fill out a multi-step listing form and submit it" (was FAILED)
- "Seller can edit listing details after publishing" (was FAILED)
- "Seller can see their listings on the seller dashboard" (was FAILED)
- "AI description polling returns updated description status" (was FAILED)
- "Seller can edit AI-generated description" (was PARTIAL — polling fix unblocks it)
</verification>

<success_criteria>
- `next build --webpack` succeeds
- All 5 response-shape gaps from VERIFICATION.md are resolved
- No caller file needs to change its destructuring logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-listing-creation-ai-core/02-08-SUMMARY.md`
</output>
