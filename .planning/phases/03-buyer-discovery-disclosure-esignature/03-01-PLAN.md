---
phase: 03-buyer-discovery-disclosure-esignature
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/types/index.ts
  - src/services/search/listings-search.ts
  - src/services/search/listings-search.test.ts
  - src/services/search/normalize.ts
  - src/services/search/normalize.test.ts
  - src/services/search/mls-client.ts
  - src/services/search/mls-client.test.ts
  - src/inngest/functions/sync-mls-listings.ts
  - src/inngest/functions/sync-mls-listings.test.ts
  - src/app/api/inngest/route.ts
  - src/app/api/search/route.ts
  - migrations/0001_add_postgis_location.sql
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-06, MLS-02, MLS-03]

must_haves:
  truths:
    - "City/zip text search returns matching active listings"
    - "Price, beds, baths, sqft, propertyType filters narrow results correctly"
    - "MLS listings from SimplyRETS ingestion appear in unified search results alongside platform listings"
    - "Search results include photo thumbnail URL, price, beds, baths, sqft, city, state"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "savedListings, savedSearches, disclosureForms, disclosureFormSchemas, signatureEnvelopes, mlsListings tables"
      contains: "mlsListings"
    - path: "src/types/index.ts"
      provides: "NormalizedListing, SearchParams types"
      exports: ["NormalizedListing", "SearchParams"]
    - path: "src/services/search/listings-search.ts"
      provides: "Unified search across listings + mlsListings tables"
      exports: ["searchListings"]
    - path: "src/services/search/normalize.ts"
      provides: "Maps listings and mlsListings rows to NormalizedListing"
      exports: ["normalizePlatformListing", "normalizeMlsListing"]
    - path: "src/services/search/mls-client.ts"
      provides: "SimplyRETS REST client for MLS data ingestion"
      exports: ["fetchSimplyRetsListings", "syncMlsListings"]
    - path: "src/app/api/search/route.ts"
      provides: "GET /api/search endpoint returning NormalizedListing[]"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/search/route.ts"
      to: "src/services/search/listings-search.ts"
      via: "import searchListings"
      pattern: "searchListings"
    - from: "src/services/search/listings-search.ts"
      to: "src/services/search/normalize.ts"
      via: "normalize both sources to NormalizedListing"
      pattern: "normalizePlatformListing|normalizeMlsListing"
    - from: "src/inngest/functions/sync-mls-listings.ts"
      to: "src/services/search/mls-client.ts"
      via: "cron triggers MLS sync"
      pattern: "syncMlsListings"
---

<objective>
Create the data foundation for Phase 3: all new Drizzle schema tables, PostGIS location column, NormalizedListing type contract, unified search service, MLS ingestion client, and search API endpoint.

Purpose: Every other Phase 3 plan depends on these schema tables and the search infrastructure. This is the horizontal foundation layer.
Output: 6 new DB tables, NormalizedListing type, search service with tests, MLS client with tests, GET /api/search endpoint.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buyer-discovery-disclosure-esignature/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema additions + PostGIS migration + NormalizedListing type contract</name>
  <files>src/db/schema.ts, src/types/index.ts, src/services/search/normalize.ts, src/services/search/normalize.test.ts, migrations/0001_add_postgis_location.sql</files>
  <behavior>
    - Test: normalizePlatformListing maps a listings row to NormalizedListing with correct fields (id, source:"platform", price, beds, baths, sqft, city, state, zip, photoUrl, lat, lng, propertyType)
    - Test: normalizeMlsListing maps an mlsListings row to NormalizedListing with source:"mls" and correct field mapping
    - Test: normalizePlatformListing handles null beds/baths/sqft (land_lot) gracefully
    - Test: normalizeMlsListing handles missing photoUrls array
  </behavior>
  <action>
    1. Extend src/db/schema.ts with ALL 6 new tables from the research doc: savedListings, savedSearches, disclosureForms, disclosureFormSchemas, signatureEnvelopes, mlsListings. Follow exact column definitions from research. Add relations for new tables (disclosureForms -> listings, signatureEnvelopes -> listings). Import `boolean` from drizzle-orm/pg-core (not currently imported).

    2. Create migrations/0001_add_postgis_location.sql with:
       - ALTER TABLE listings ADD COLUMN IF NOT EXISTS location geometry(point, 4326);
       - CREATE INDEX IF NOT EXISTS listings_location_gist ON listings USING GIST(location);
       Note: Same raw SQL migration pattern used for pgvector in Phase 2.

    3. Add to src/types/index.ts:
       - NormalizedListing interface: { id: string; source: "platform" | "mls"; streetAddress?: string; city: string; state: string; zip: string; price: number; bedrooms: number | null; bathrooms: number | null; sqft: number | null; propertyType: string; photoUrl: string | null; lat: number | null; lng: number | null; status: string; createdAt: string }
       - SearchParams type: { q?: string; minPrice?: number; maxPrice?: number; minBeds?: number; maxBeds?: number; minBaths?: number; maxBaths?: number; minSqft?: number; maxSqft?: number; propertyType?: PropertyType; lat?: number; lng?: number; radiusKm?: number; page?: number; limit?: number }

    4. Create src/services/search/normalize.ts with normalizePlatformListing and normalizeMlsListing functions. Platform listing uses first item in photoOrder to construct photoUrl from NEXT_PUBLIC_R2_PUBLIC_URL. MLS listing uses first item in photoUrls array.

    5. Write tests in src/services/search/normalize.test.ts per behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/services/search/normalize.test.ts --reporter=verbose</automated>
  </verify>
  <done>6 new tables in schema.ts, PostGIS migration SQL file exists, NormalizedListing + SearchParams types exported, normalize functions pass all tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unified search service + MLS client + search API endpoint</name>
  <files>src/services/search/listings-search.ts, src/services/search/listings-search.test.ts, src/services/search/mls-client.ts, src/services/search/mls-client.test.ts, src/inngest/functions/sync-mls-listings.ts, src/inngest/functions/sync-mls-listings.test.ts, src/app/api/inngest/route.ts, src/app/api/search/route.ts</files>
  <behavior>
    - Test: searchListings with q="Austin" returns listings with city matching "Austin"
    - Test: searchListings with minPrice/maxPrice filters narrows results
    - Test: searchListings with minBeds filter returns only listings with >= N beds
    - Test: searchListings returns both platform and MLS listings normalized
    - Test: fetchSimplyRetsListings calls SimplyRETS API with correct params (mock HTTP)
    - Test: syncMlsListings upserts fetched records into mlsListings table shape (mock HTTP + mock DB)
    - Test: Inngest sync function calls syncMlsListings
  </behavior>
  <action>
    1. Create src/services/search/listings-search.ts with searchListings(params: SearchParams) function:
       - Build Drizzle WHERE conditions from SearchParams (city/zip ilike for q, gte/lte for price/beds/baths/sqft, eq for propertyType)
       - If lat+lng provided, add PostGIS ST_DWithin condition using raw sql`` template (cast to ::geography for spherical distance)
       - Query listings table (status = "active") with conditions, limit, offset
       - Query mlsListings table with equivalent conditions (status = "Active")
       - Normalize both result sets via normalize functions
       - Return combined NormalizedListing[] sorted by createdAt desc
       - Return { results: NormalizedListing[], total: number, page: number }

    2. Write tests in src/services/search/listings-search.test.ts. Mock the db module. Test filter logic by verifying the correct Drizzle conditions are constructed. Do NOT test against a real database.

    3. Create src/services/search/mls-client.ts:
       - fetchSimplyRetsListings(params): fetches from https://api.simplyrets.com/properties with Basic auth (SIMPLYRETS_API_KEY:SIMPLYRETS_API_SECRET). Params: status=Active, limit, offset, q (city), minprice, maxprice, minbeds, type.
       - syncMlsListings(): calls fetchSimplyRetsListings paginated, upserts into mlsListings table. Uses the mlsId from SimplyRETS as the row id. Maps SimplyRETS fields to mlsListings columns.

    4. Write tests in src/services/search/mls-client.test.ts. Mock global fetch. Verify correct URL construction, auth header, and response mapping.

    5. Create src/inngest/functions/sync-mls-listings.ts with syncMlsListingsCron Inngest function (cron: "0 */4 * * *" — every 4 hours). Uses step.run to call syncMlsListings. Export both the raw async function (for tests) and the wrapped Inngest function.

    6. Write test in src/inngest/functions/sync-mls-listings.test.ts verifying the raw function calls syncMlsListings.

    7. Register syncMlsListingsCron in src/app/api/inngest/route.ts functions array (add to existing generateListingDescriptionFn).

    8. Create src/app/api/search/route.ts with GET handler:
       - Parse SearchParams from URL searchParams
       - Call searchListings(params)
       - Return NextResponse.json(result)
       - No auth required (public endpoint)

    9. Add SIMPLYRETS_API_KEY and SIMPLYRETS_API_SECRET to .env.example.
  </action>
  <verify>
    <automated>npx vitest run src/services/search/ src/inngest/functions/sync-mls-listings.test.ts --reporter=verbose && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>searchListings returns filtered NormalizedListing[] from both sources, MLS client fetches and syncs SimplyRETS data, Inngest cron registered, GET /api/search returns JSON, all tests pass, build clean</done>
</task>

</tasks>

<verification>
- All new tests pass: `npx vitest run src/services/search/ --reporter=verbose`
- Build succeeds: `npx next build 2>&1 | tail -20`
- Schema file exports all 6 new tables
- NormalizedListing type exported from src/types/index.ts
- GET /api/search endpoint exists and returns JSON
</verification>

<success_criteria>
- City/zip text search returns matching listings
- Price/beds/baths/type filters narrow results
- MLS and platform listings both appear in unified results as NormalizedListing
- All Phase 3 schema tables defined and ready for downstream plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-buyer-discovery-disclosure-esignature/03-01-SUMMARY.md`
</output>
