---
phase: 03-buyer-discovery-disclosure-esignature
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/services/disclosures/form-schema.ts
  - src/services/disclosures/form-schema.test.ts
  - src/services/disclosures/disclosure-form.ts
  - src/services/disclosures/disclosure-form.test.ts
  - src/services/disclosures/ai-assist.ts
  - src/services/disclosures/ai-assist.test.ts
  - src/app/api/disclosures/route.ts
  - src/app/api/disclosures/[id]/route.ts
  - src/app/api/disclosures/ai-assist/route.ts
  - src/components/disclosures/DisclosureForm.tsx
  - src/components/disclosures/AiFormAssistant.tsx
  - src/app/seller/disclosures/[listingId]/page.tsx
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04]

must_haves:
  truths:
    - "Platform provides state-specific disclosure form schemas for all 10 launch states"
    - "Seller can fill out a disclosure form digitally within the platform using dynamic fields from the JSON schema"
    - "AI assists with disclosure form completion via streaming guidance with UPL disclaimer on every response"
    - "Completed disclosure form answers are stored in the database and linked to the listing"
  artifacts:
    - path: "src/services/disclosures/form-schema.ts"
      provides: "Per-state disclosure form schema registry with attorney_reviewed gate"
      exports: ["getFormSchemaForState", "getAvailableFormSchemas", "LAUNCH_STATE_SCHEMAS"]
    - path: "src/services/disclosures/disclosure-form.ts"
      provides: "CRUD for disclosure form answers"
      exports: ["createDisclosureForm", "updateDisclosureForm", "getDisclosureForm", "completeDisclosureForm"]
    - path: "src/services/disclosures/ai-assist.ts"
      provides: "AI streaming guidance for form field completion"
      exports: ["createDisclosureAssistStream"]
    - path: "src/components/disclosures/DisclosureForm.tsx"
      provides: "Dynamic form renderer from JSON schema"
      min_lines: 50
    - path: "src/components/disclosures/AiFormAssistant.tsx"
      provides: "Streaming AI chat panel for form help"
      min_lines: 30
    - path: "src/app/seller/disclosures/[listingId]/page.tsx"
      provides: "Disclosure form page for a listing"
      min_lines: 30
  key_links:
    - from: "src/components/disclosures/DisclosureForm.tsx"
      to: "src/services/disclosures/form-schema.ts"
      via: "loads form schema by state to render fields"
      pattern: "getFormSchemaForState"
    - from: "src/services/disclosures/disclosure-form.ts"
      to: "src/db/schema.ts"
      via: "reads/writes disclosureForms table"
      pattern: "disclosureForms"
    - from: "src/services/disclosures/ai-assist.ts"
      to: "ai"
      via: "AI SDK streaming with UPL system prompt"
      pattern: "streamText|toUIMessageStreamResponse"
    - from: "src/app/api/disclosures/ai-assist/route.ts"
      to: "src/services/disclosures/ai-assist.ts"
      via: "import createDisclosureAssistStream"
      pattern: "createDisclosureAssistStream"
---

<objective>
Build the state disclosure form system: per-state JSON form schemas for all 10 launch states, digital form filling UI, disclosure form CRUD API, and AI-assisted form completion with streaming guidance.

Purpose: State disclosure forms are legally required in 9 of 10 launch states (GA optional). Digital completion within the platform eliminates paper forms and enables the AI assistant to guide sellers through each field.
Output: Form schema registry, dynamic DisclosureForm component, AI assistant widget, disclosure API, seller disclosure page.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buyer-discovery-disclosure-esignature/03-RESEARCH.md

<interfaces>
<!-- From Plan 03-01 schema outputs -->

From src/db/schema.ts:
```typescript
export const disclosureForms = pgTable("disclosure_forms", {
  id: text("id").primaryKey(),
  listingId: text("listing_id").notNull().references(() => listings.id),
  userId: text("user_id").notNull(),
  state: text("state").notNull(),
  formSchemaId: text("form_schema_id").notNull(),
  answers: text("answers").notNull(), // JSON blob
  status: text("status").default("draft").notNull(),
  completedAt: timestamp("completed_at"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});

export const disclosureFormSchemas = pgTable("disclosure_form_schemas", {
  id: text("id").primaryKey(),
  state: text("state").notNull(),
  formName: text("form_name").notNull(),
  version: text("version").notNull(),
  fields: text("fields").notNull(), // JSON schema
  required: boolean("required").default(true).notNull(),
  attorneyReviewedAt: timestamp("attorney_reviewed_at"),
  attorneyName: text("attorney_name"),
  active: boolean("active").default(false).notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```

From Phase 2 AI SDK pattern (02-06-SUMMARY):
```typescript
// AI SDK v6 uses streamText + toUIMessageStreamResponse
import { streamText } from "ai";
import { openai } from "@ai-sdk/openai";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Form schema registry + disclosure form CRUD + AI assist service</name>
  <files>src/services/disclosures/form-schema.ts, src/services/disclosures/form-schema.test.ts, src/services/disclosures/disclosure-form.ts, src/services/disclosures/disclosure-form.test.ts, src/services/disclosures/ai-assist.ts, src/services/disclosures/ai-assist.test.ts</files>
  <behavior>
    - Test: getFormSchemaForState("CA") returns California TDS schema with correct formName and required=true
    - Test: getFormSchemaForState("GA") returns Georgia schema with required=false
    - Test: getAvailableFormSchemas returns schemas for all 10 launch states
    - Test: LAUNCH_STATE_SCHEMAS has entries for CA, TX, FL, NY, GA, NC, AZ, OH, PA, IL
    - Test: createDisclosureForm stores answers as JSON string linked to listingId
    - Test: updateDisclosureForm updates answers for existing form
    - Test: completeDisclosureForm sets status to "complete" and completedAt timestamp
    - Test: getDisclosureForm returns form with parsed answers
    - Test: AI assist system prompt includes UPL disclaimer text
    - Test: AI assist system prompt includes the state name and form context
  </behavior>
  <action>
    1. Create src/services/disclosures/form-schema.ts:
       - Define LAUNCH_STATE_SCHEMAS as a Record<string, DisclosureFormSchemaTemplate> for all 10 launch states. Each entry has: state, formName, version "1.0", required (true for all except GA), fields as a JSON string containing an array of field objects.
       - Field objects shape: { id: string, label: string, type: "text" | "textarea" | "boolean" | "select", section: string, required: boolean, helpText?: string, options?: string[] }
       - For each state, include 5-10 representative disclosure fields covering the key statutory requirements noted in the research doc. These are PLACEHOLDER fields that must be replaced after attorney review. Mark each schema with a comment: "PLACEHOLDER — requires attorney review before activation."
       - CA fields: property condition, structural issues, roof age, plumbing, electrical, lead paint (pre-1978), natural hazards, environmental, neighborhood noise, HOA
       - TX fields: follow TREC Form OP-H structure — foundation, roof, walls, plumbing, electrical, equipment, flooding history, environmental
       - FL fields: known defects, radon, HOA, coastal erosion, sinkholes, flood zone
       - NY fields: property condition (or $500 credit option — model as a boolean "opt_out_with_credit" field), structural, environmental, lead paint
       - GA fields: property condition, structural, systems (with required=false on the schema level — seller can decline)
       - NC fields: property condition with "No Representation" as a valid select option for each field
       - AZ (SPDS): detailed structure, HOA, environmental, utilities, solar
       - OH: statutory questions per ORC 5302.30
       - PA: Act 114 disclosure questions
       - IL: 23 statutory questions from 765 ILCS 77
       - getFormSchemaForState(state: string): looks up LAUNCH_STATE_SCHEMAS, returns the schema template or null.
       - getAvailableFormSchemas(): returns all 10 schema templates.

    2. Write tests in src/services/disclosures/form-schema.test.ts.

    3. Create src/services/disclosures/disclosure-form.ts:
       - createDisclosureForm(userId, listingId, state): looks up schema for state, inserts disclosureForms row with empty answers JSON, returns the form record.
       - updateDisclosureForm(userId, formId, answers: Record<string, unknown>): updates answers JSON string on the row. Ownership check: userId must match.
       - completeDisclosureForm(userId, formId): sets status="complete", completedAt=new Date(). Ownership check.
       - getDisclosureForm(formId): returns row with answers parsed from JSON.

    4. Write tests in src/services/disclosures/disclosure-form.test.ts mocking the db module.

    5. Create src/services/disclosures/ai-assist.ts:
       - createDisclosureAssistStream(state: string, formName: string, fieldContext: string, userMessage: string): uses AI SDK v6 streamText with openai("gpt-4o-mini") model.
       - System prompt: "You are helping a user understand what information to provide in the {formName} disclosure form for {state}. Current field: {fieldContext}. You are NOT providing legal advice. This is general informational guidance only. Always recommend consulting a licensed real estate attorney for legal guidance specific to your situation. Every response must include: 'This is not legal advice. Consult a licensed attorney.'"
       - Returns the stream response.

    6. Write tests in src/services/disclosures/ai-assist.test.ts: verify system prompt contains UPL disclaimer text and state name. Mock the AI SDK streamText function.
  </action>
  <verify>
    <automated>npx vitest run src/services/disclosures/ --reporter=verbose</automated>
  </verify>
  <done>Form schemas exist for all 10 states, disclosure CRUD works, AI assist includes UPL disclaimers, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Disclosure API endpoints + form UI + AI assistant widget + seller page</name>
  <files>src/app/api/disclosures/route.ts, src/app/api/disclosures/[id]/route.ts, src/app/api/disclosures/ai-assist/route.ts, src/components/disclosures/DisclosureForm.tsx, src/components/disclosures/AiFormAssistant.tsx, src/app/seller/disclosures/[listingId]/page.tsx</files>
  <action>
    1. Create src/app/api/disclosures/route.ts:
       - POST: auth required, body { listingId, state }, calls createDisclosureForm, returns 201 with form record
       - GET: auth required, query param listingId, returns all disclosure forms for that listing

    2. Create src/app/api/disclosures/[id]/route.ts:
       - GET: auth required, calls getDisclosureForm, returns form with parsed answers
       - PATCH: auth required, body { answers }, calls updateDisclosureForm, returns updated form
       - POST with action="complete": auth required, calls completeDisclosureForm, returns completed form

    3. Create src/app/api/disclosures/ai-assist/route.ts:
       - POST: auth required, body { state, formName, fieldContext, message }
       - Calls createDisclosureAssistStream, returns the streaming response
       - Same streaming response pattern as Phase 2 chatbot (toUIMessageStreamResponse)

    4. Create src/components/disclosures/DisclosureForm.tsx ('use client'):
       - Props: { formId: string; state: string; fields: FieldSchema[]; initialAnswers: Record<string, unknown>; onSave: (answers) => void; onComplete: () => void }
       - Uses react-hook-form to manage field state
       - Dynamically renders fields based on type: text -> input, textarea -> textarea, boolean -> checkbox, select -> select dropdown
       - Groups fields by section with section headings
       - "Save Draft" button calls onSave with current form values (PATCH API)
       - "Complete & Submit" button validates all required fields, calls onComplete (POST complete action)
       - Shows field helpText below each input
       - For NY: if opt_out_with_credit is checked, show message "You have opted to provide a $500 credit instead of completing this form" and disable other fields
       - For NC: select fields include "No Representation" as a valid option
       - For GA: show banner "Disclosure is optional in Georgia. You may skip this form."

    5. Create src/components/disclosures/AiFormAssistant.tsx ('use client'):
       - Props: { state: string; formName: string; currentField?: string }
       - Floating panel (right sidebar or bottom drawer on mobile)
       - Text input for user questions about the current field
       - Streams AI response via fetch to /api/disclosures/ai-assist
       - Every AI response is preceded by a disclaimer banner: "AI guidance only — not legal advice"
       - Uses useChat from @ai-sdk/react with DefaultChatTransport (same pattern as Phase 2 chatbot)

    6. Create src/app/seller/disclosures/[listingId]/page.tsx:
       - Server component that fetches listing by id (verify ownership), gets disclosure forms for listing
       - If no form exists for the listing's state, shows "Start Disclosure" button (creates form via API)
       - If form exists, renders DisclosureForm + AiFormAssistant side by side
       - Shows form status (draft/complete) and completedAt if complete
       - If form schema has required=false (GA), shows option to skip

    7. Ensure build passes.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Disclosure form page renders at /seller/disclosures/[listingId], dynamic form fields from JSON schema, AI assistant panel streams guidance with UPL disclaimers, save draft and complete actions work, build clean</done>
</task>

</tasks>

<verification>
- Tests pass: `npx vitest run src/services/disclosures/ --reporter=verbose`
- Build succeeds: `npx next build 2>&1 | tail -20`
- All 10 launch state schemas defined in form-schema.ts
- Disclosure form CRUD API at /api/disclosures
- AI assist API at /api/disclosures/ai-assist with streaming
- DisclosureForm renders dynamic fields from JSON schema
- AiFormAssistant streams responses with UPL disclaimers
</verification>

<success_criteria>
- All 10 launch state disclosure form schemas exist (marked as attorney review pending)
- Seller can start, fill, save draft, and complete a disclosure form for their listing's state
- AI assistant provides field-level guidance with "not legal advice" disclaimer on every response
- Completed forms are stored as JSON in the database linked to the listing
- GA shows optional disclosure banner; NY shows $500 credit opt-out option
</success_criteria>

<output>
After completion, create `.planning/phases/03-buyer-discovery-disclosure-esignature/03-04-SUMMARY.md`
</output>
