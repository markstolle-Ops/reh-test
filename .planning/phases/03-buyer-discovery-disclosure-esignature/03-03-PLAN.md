---
phase: 03-buyer-discovery-disclosure-esignature
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/search/MapView.tsx
  - src/app/buyer/search/page.tsx
  - src/app/api/saved-searches/route.ts
  - src/app/api/saved-searches/[id]/route.ts
  - src/services/search/saved-searches.ts
  - src/services/search/saved-searches.test.ts
  - src/inngest/functions/match-saved-searches.ts
  - src/inngest/functions/match-saved-searches.test.ts
  - src/app/api/inngest/route.ts
  - package.json
autonomous: true
requirements: [SRCH-03, SRCH-04]

must_haves:
  truths:
    - "Buyer can view search results on an interactive Mapbox map with listing markers showing price"
    - "Buyer can save a search with current filters and receive email alerts when new matching listings appear"
    - "Saved search alert cron fans out per-user, checks for new listings since last alert, sends Resend email"
  artifacts:
    - path: "src/components/search/MapView.tsx"
      provides: "Mapbox GL JS interactive map with listing markers"
      min_lines: 30
    - path: "src/services/search/saved-searches.ts"
      provides: "CRUD for saved search records"
      exports: ["createSavedSearch", "getSavedSearches", "deleteSavedSearch"]
    - path: "src/inngest/functions/match-saved-searches.ts"
      provides: "Daily cron + per-search fan-out for email alerts"
      exports: ["matchSavedSearchesCron", "checkSavedSearchAlert"]
    - path: "src/app/api/saved-searches/route.ts"
      provides: "POST create, GET list saved searches"
      exports: ["POST", "GET"]
  key_links:
    - from: "src/components/search/MapView.tsx"
      to: "src/types/index.ts"
      via: "NormalizedListing[] for marker positions"
      pattern: "NormalizedListing"
    - from: "src/inngest/functions/match-saved-searches.ts"
      to: "src/services/search/listings-search.ts"
      via: "re-runs saved filter as searchListings query"
      pattern: "searchListings"
    - from: "src/inngest/functions/match-saved-searches.ts"
      to: "resend"
      via: "sends alert email via Resend"
      pattern: "resend.*send"
---

<objective>
Add the interactive map view to the search page and implement saved search with email alert functionality.

Purpose: Map view is essential for geographic property discovery. Saved search alerts keep buyers engaged and returning to the platform.
Output: MapView component on search page, saved search CRUD + API, Inngest cron for matching + email alerts.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-buyer-discovery-disclosure-esignature/03-RESEARCH.md

<interfaces>
<!-- From Plan 03-01 outputs -->

From src/types/index.ts:
```typescript
export interface NormalizedListing {
  id: string;
  source: "platform" | "mls";
  city: string;
  state: string;
  zip: string;
  price: number;
  bedrooms: number | null;
  bathrooms: number | null;
  sqft: number | null;
  propertyType: string;
  photoUrl: string | null;
  lat: number | null;
  lng: number | null;
  status: string;
  createdAt: string;
}

export type SearchParams = { q?: string; minPrice?: number; maxPrice?: number; ... };
```

From src/db/schema.ts:
```typescript
export const savedSearches = pgTable("saved_searches", {
  id: text("id").primaryKey(),
  userId: text("user_id").notNull(),
  name: text("name").notNull(),
  filters: text("filters").notNull(), // JSON blob of SearchParams
  active: boolean("active").default(true).notNull(),
  lastAlertSentAt: timestamp("last_alert_sent_at"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
});
```

From src/services/search/listings-search.ts:
```typescript
export async function searchListings(params: SearchParams): Promise<{ results: NormalizedListing[]; total: number; page: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapbox MapView component + search page tab integration</name>
  <files>src/components/search/MapView.tsx, src/app/buyer/search/page.tsx, package.json</files>
  <action>
    1. Install Mapbox dependencies: `npm install mapbox-gl react-map-gl` and `npm install -D @types/mapbox-gl`

    2. Create src/components/search/MapView.tsx ('use client'):
       - Props: { listings: NormalizedListing[]; onMarkerClick?: (id: string) => void }
       - Import Map, Marker, NavigationControl from 'react-map-gl'
       - Import 'mapbox-gl/dist/mapbox-gl.css' for map styles
       - mapboxAccessToken from process.env.NEXT_PUBLIC_MAPBOX_TOKEN
       - initialViewState: center of US (longitude: -98.5, latitude: 38.9, zoom: 4)
       - Map style: "mapbox://styles/mapbox/streets-v12"
       - Render Marker for each listing that has non-null lat/lng
       - Marker content: small white rounded pill showing price formatted as "$XXXk" or "$X.Xm"
       - Clicking a marker calls onMarkerClick with the listing id
       - Size: width 100%, height 600px
       - Include NavigationControl (zoom buttons)

    3. Update src/app/buyer/search/page.tsx to add list/map toggle:
       - Add a "List | Map" toggle button pair above the results area
       - When "List" is selected, show SearchResults grid (existing)
       - When "Map" is selected, show MapView with the same listings
       - Use dynamic import with { ssr: false } for MapView to prevent SSR issues with mapbox-gl
       - State: const [view, setView] = useState<"list" | "map">("list")

    4. Add NEXT_PUBLIC_MAPBOX_TOKEN to .env.example.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>MapView renders on search page with listing markers, list/map toggle works, build clean, NEXT_PUBLIC_MAPBOX_TOKEN documented in .env.example</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Saved search CRUD + Inngest alert cron with email fan-out</name>
  <files>src/services/search/saved-searches.ts, src/services/search/saved-searches.test.ts, src/inngest/functions/match-saved-searches.ts, src/inngest/functions/match-saved-searches.test.ts, src/app/api/saved-searches/route.ts, src/app/api/saved-searches/[id]/route.ts, src/app/api/inngest/route.ts</files>
  <behavior>
    - Test: createSavedSearch stores name + JSON-stringified filters + userId
    - Test: getSavedSearches returns all active searches for a user
    - Test: deleteSavedSearch removes the record
    - Test: matchSavedSearches raw function loads active searches and produces fan-out events
    - Test: checkSavedSearchAlert raw function calls searchListings with stored filters and sends email if new results exist
  </behavior>
  <action>
    1. Create src/services/search/saved-searches.ts:
       - createSavedSearch(userId: string, name: string, filters: SearchParams): inserts into savedSearches table with JSON.stringify(filters), returns the record.
       - getSavedSearches(userId: string): selects all active saved searches for user.
       - deleteSavedSearch(userId: string, id: string): deletes if userId matches.

    2. Write tests in src/services/search/saved-searches.test.ts mocking the db module.

    3. Create src/inngest/functions/match-saved-searches.ts:
       - Export matchSavedSearchesCron: Inngest cron function (id: "match-saved-searches-cron", cron: "0 9 * * *" — daily 9am UTC). step.run loads all active savedSearches. step.sendEvent fans out "search/alert.check" events with { savedSearchId, userId, filters }.
       - Export checkSavedSearchAlert: Inngest event function (id: "check-saved-search-alert", event: "search/alert.check"). Parses filters from event.data, calls searchListings with those filters, filters results to only those created after lastAlertSentAt, if any new results exist: sends email via Resend with listing summary, updates lastAlertSentAt.
       - Export raw async functions for both (for testing), wrap with inngest.createFunction for registration.
       - Email template: subject "New listings matching your search '{name}'", body lists up to 5 new listings with price and city.

    4. Write tests in src/inngest/functions/match-saved-searches.test.ts for the raw functions. Mock searchListings and Resend.

    5. Create src/app/api/saved-searches/route.ts:
       - POST: auth required, body { name, filters }, calls createSavedSearch, returns 201
       - GET: auth required, calls getSavedSearches, returns array

    6. Create src/app/api/saved-searches/[id]/route.ts:
       - DELETE: auth required, calls deleteSavedSearch, returns 200

    7. Register matchSavedSearchesCron and checkSavedSearchAlert in src/app/api/inngest/route.ts functions array.
  </action>
  <verify>
    <automated>npx vitest run src/services/search/saved-searches.test.ts src/inngest/functions/match-saved-searches.test.ts --reporter=verbose && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Saved search CRUD works via API, Inngest cron fans out alerts daily, email sent via Resend for new matches, all tests pass, build clean</done>
</task>

</tasks>

<verification>
- Tests pass: `npx vitest run src/services/search/saved-searches.test.ts src/inngest/functions/match-saved-searches.test.ts --reporter=verbose`
- Build succeeds: `npx next build 2>&1 | tail -20`
- MapView component exists and is dynamically imported in search page
- Saved search API endpoints exist at /api/saved-searches
- Inngest route registers both new cron functions
</verification>

<success_criteria>
- Map view shows listing markers with price labels on Mapbox map
- List/map toggle switches views on the search page
- Buyer can save a search and it persists in the database
- Inngest cron matches new listings against saved searches and sends email alerts
</success_criteria>

<output>
After completion, create `.planning/phases/03-buyer-discovery-disclosure-esignature/03-03-SUMMARY.md`
</output>
