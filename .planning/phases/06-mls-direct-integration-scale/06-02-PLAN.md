---
phase: 06-mls-direct-integration-scale
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow/states/index.ts
  - src/workflow/states/AL.ts
  - src/workflow/states/AK.ts
  - src/workflow/states/AR.ts
  - src/workflow/states/CO.ts
  - src/workflow/states/CT.ts
  - src/workflow/states/DE.ts
  - src/workflow/states/HI.ts
  - src/workflow/states/ID.ts
  - src/workflow/states/IN.ts
  - src/workflow/states/IA.ts
  - src/workflow/states/KS.ts
  - src/workflow/states/KY.ts
  - src/workflow/states/LA.ts
  - src/workflow/states/ME.ts
  - src/workflow/states/MD.ts
  - src/workflow/states/MA.ts
  - src/workflow/states/MI.ts
  - src/workflow/states/MN.ts
  - src/workflow/states/MS.ts
  - src/workflow/states/MO.ts
  - src/workflow/states/MT.ts
  - src/workflow/states/NE.ts
  - src/workflow/states/NV.ts
  - src/workflow/states/NH.ts
  - src/workflow/states/NJ.ts
  - src/workflow/states/NM.ts
  - src/workflow/states/ND.ts
  - src/workflow/states/OK.ts
  - src/workflow/states/OR.ts
  - src/workflow/states/RI.ts
  - src/workflow/states/SC.ts
  - src/workflow/states/SD.ts
  - src/workflow/states/TN.ts
  - src/workflow/states/UT.ts
  - src/workflow/states/VT.ts
  - src/workflow/states/VA.ts
  - src/workflow/states/WA.ts
  - src/workflow/states/WV.ts
  - src/workflow/states/WI.ts
  - src/workflow/states/WY.ts
  - src/workflow/states/DC.ts
  - src/lib/states.ts
  - src/lib/constants.ts
  - src/services/disclosures/form-schema.ts
  - src/services/disclosures/form-schema.test.ts
autonomous: true
requirements: [MLS-04]

must_haves:
  truths:
    - "Platform supports all 50 US states plus DC with workflow configs"
    - "Every state has a disclosure form schema in LAUNCH_STATE_SCHEMAS"
    - "STATE_INFO in lib/states.ts covers all 50 states plus DC"
    - "getStateWorkflowConfig does not throw for any valid US state code"
  artifacts:
    - path: "src/workflow/states/index.ts"
      provides: "ALL_STATE_CONFIGS registry with 51 entries (50 states + DC)"
      contains: "ALL_STATE_CONFIGS"
    - path: "src/lib/states.ts"
      provides: "STATE_INFO with 51 entries"
      contains: "STATE_INFO"
    - path: "src/services/disclosures/form-schema.ts"
      provides: "LAUNCH_STATE_SCHEMAS with 51 entries"
      contains: "LAUNCH_STATE_SCHEMAS"
  key_links:
    - from: "src/workflow/states/index.ts"
      to: "src/workflow/types.ts"
      via: "StateWorkflowConfig type conformance for all 41 new configs"
      pattern: "satisfies StateWorkflowConfig"
    - from: "src/lib/states.ts"
      to: "src/types/index.ts"
      via: "ClosingType union used in all StateInfo entries"
      pattern: "closingType"
---

<objective>
Expand platform coverage from 10 launch states to all 50 US states plus DC with complete workflow configs, disclosure form schemas, and state info entries.

Purpose: Full 50-state coverage is required for MLS-04 (top 10-15 highest-volume markets span states beyond the original 10). The platform must not throw errors or show blank pages when a user enters a transaction in any US state.

Output: 41 new state workflow config files, expanded disclosure form registry, expanded state info registry, updated constants.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key existing code:
- src/workflow/states/CA.ts — example config shape (satisfies StateWorkflowConfig)
- src/workflow/states/index.ts — ALL_STATE_CONFIGS registry (10 entries)
- src/workflow/types.ts — StateWorkflowConfig, WorkflowStep interfaces
- src/lib/states.ts — STATE_INFO registry (10 entries)
- src/lib/constants.ts — LAUNCH_STATES array (10 entries)
- src/services/disclosures/form-schema.ts — LAUNCH_STATE_SCHEMAS (10 entries), FieldSchema/DisclosureFormSchemaTemplate types

<interfaces>
From src/workflow/types.ts:
```typescript
export interface WorkflowStep {
  id: string;
  label: string;
  requiredDocuments: string[];
  deadlineDays?: number;
  deadlineType?: "inspection" | "financing" | "closing_disclosure" | "closing";
  triggers?: string[];
}

export interface StateWorkflowConfig {
  stateCode: string;
  stateName: string;
  closingType: ClosingType;
  fsboAllowed: boolean;
  ronAvailable: boolean;
  steps: WorkflowStep[];
  legalRequirementsSummary: string;
  attorneyReferralRequired: boolean;
  partnerBrokerRequired: boolean;
}
```

From src/services/disclosures/form-schema.ts:
```typescript
export interface FieldSchema {
  id: string;
  label: string;
  type: "text" | "textarea" | "boolean" | "select";
  section: string;
  required: boolean;
  helpText?: string;
  options?: string[];
}

export interface DisclosureFormSchemaTemplate {
  state: string;
  formName: string;
  version: string;
  required: boolean;
  fields: string; // JSON-encoded FieldSchema[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 41 new state workflow configs + update registry</name>
  <files>
    src/workflow/states/AL.ts, src/workflow/states/AK.ts, src/workflow/states/AR.ts,
    src/workflow/states/CO.ts, src/workflow/states/CT.ts, src/workflow/states/DE.ts,
    src/workflow/states/HI.ts, src/workflow/states/ID.ts, src/workflow/states/IN.ts,
    src/workflow/states/IA.ts, src/workflow/states/KS.ts, src/workflow/states/KY.ts,
    src/workflow/states/LA.ts, src/workflow/states/ME.ts, src/workflow/states/MD.ts,
    src/workflow/states/MA.ts, src/workflow/states/MI.ts, src/workflow/states/MN.ts,
    src/workflow/states/MS.ts, src/workflow/states/MO.ts, src/workflow/states/MT.ts,
    src/workflow/states/NE.ts, src/workflow/states/NV.ts, src/workflow/states/NH.ts,
    src/workflow/states/NJ.ts, src/workflow/states/NM.ts, src/workflow/states/ND.ts,
    src/workflow/states/OK.ts, src/workflow/states/OR.ts, src/workflow/states/RI.ts,
    src/workflow/states/SC.ts, src/workflow/states/SD.ts, src/workflow/states/TN.ts,
    src/workflow/states/UT.ts, src/workflow/states/VT.ts, src/workflow/states/VA.ts,
    src/workflow/states/WA.ts, src/workflow/states/WV.ts, src/workflow/states/WI.ts,
    src/workflow/states/WY.ts, src/workflow/states/DC.ts,
    src/workflow/states/index.ts
  </files>
  <action>
    Create one TypeScript file per state following the exact same shape as CA.ts (satisfies StateWorkflowConfig).

    State classification reference for closingType and attorneyReferralRequired:
    - attorney-required (attorneyReferralRequired: true): CT, DE, GA, MA, NC, SC, VT, WV (+ existing GA, NC)
    - customary-attorney (attorneyReferralRequired: true): NY, IL, NJ, RI, ME, NH (+ existing NY, IL)
    - title-company (attorneyReferralRequired: false): all others

    FSBO allowed: true for ALL states (every US state allows FSBO; some require broker disclosure)

    RON available: true for most states as of 2025. Exceptions (ronAvailable: false): CA (pending legislation as of 2025)

    partnerBrokerRequired: true ONLY for NY (per existing STATE.md decision). All others false.

    Each state file exports a named config (e.g., alConfig, akConfig) with:
    - Accurate legalRequirementsSummary (1-2 sentences covering closing type, attorney requirement, key disclosure)
    - Standard 7-step workflow (same step IDs as CA.ts: offer_submitted, offer_accepted, inspection_period, financing_period, closing_disclosure, pending_closing, closed)
    - Attorney-required states add attorney_review step between offer_accepted and inspection_period (same as GA.ts pattern)
    - Standard deadlineDays: inspection 10, financing 21, closing_disclosure 3, closing 30

    Update src/workflow/states/index.ts:
    - Import all 41 new configs
    - Add all to ALL_STATE_CONFIGS record (total: 51 entries — 50 states + DC)
    - getStateWorkflowConfig now works for any US state code

    IMPORTANT: These are PLACEHOLDER configs (same caveat as disclosure forms). Each requires attorney review before production activation. Add comment at top of each file: "// PLACEHOLDER — requires attorney review before production activation"
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && node -e "const s = require('./src/workflow/states/index.ts'); console.log(Object.keys(s.ALL_STATE_CONFIGS).length)" 2>/dev/null || npx tsx -e "import { ALL_STATE_CONFIGS } from './src/workflow/states/index.ts'; console.log('States:', Object.keys(ALL_STATE_CONFIGS).length); if (Object.keys(ALL_STATE_CONFIGS).length !== 51) throw new Error('Expected 51 configs');" 2>&1 | tail -10</automated>
  </verify>
  <done>51 state workflow configs (50 states + DC) in ALL_STATE_CONFIGS registry. getStateWorkflowConfig returns valid config for any US state code. Attorney-required states have attorney_review step. All configs satisfy StateWorkflowConfig type.</done>
</task>

<task type="auto">
  <name>Task 2: Expand disclosure schemas + state info + constants</name>
  <files>
    src/services/disclosures/form-schema.ts,
    src/services/disclosures/form-schema.test.ts,
    src/lib/states.ts,
    src/lib/constants.ts
  </files>
  <action>
    1. Expand LAUNCH_STATE_SCHEMAS in src/services/disclosures/form-schema.ts:
    Add 41 new entries (one per new state + DC). Each entry follows existing DisclosureFormSchemaTemplate shape.
    - All new schemas have version: "1.0", required: true (except states with optional disclosure like Indiana)
    - Each state gets 4-8 fields covering: structural issues, roof condition, environmental hazards, plumbing/electrical, HOA, and state-specific items (e.g., radon for mountain states, sinkholes for Florida already covered, earthquake for Pacific Northwest, termites for Southern states)
    - Retain the "PLACEHOLDER" comment at file top — all schemas require attorney review
    - getFormSchemaForState now returns non-null for any US state code
    - getAvailableFormSchemas returns 51 entries

    2. Expand STATE_INFO in src/lib/states.ts:
    Add 41 new entries. Each StateInfo has:
    - code, name, closingType (matching workflow config classification)
    - estimatedTitleFeePercent: 0.006-0.008 range based on state (use 0.007 as default for most, 0.006 for attorney states, 0.008 for high-cost states like HI, CT, NJ)

    3. Update src/lib/constants.ts:
    - Rename LAUNCH_STATES to ALL_STATES (or add ALL_STATES alongside LAUNCH_STATES for backward compat)
    - Add ALL_STATES array with all 51 codes
    - Keep LAUNCH_STATES for backward compatibility (existing code references it)
    - Add ATTORNEY_REQUIRED_STATES (expanded from ATTORNEY_STATES): ["CT", "DE", "GA", "MA", "NC", "SC", "VT", "WV"]
    - Add CUSTOMARY_ATTORNEY_STATES (expanded): ["IL", "ME", "NH", "NJ", "NY", "RI"]

    4. Update existing tests in form-schema.test.ts:
    - Verify getAvailableFormSchemas().length === 51
    - Verify getFormSchemaForState returns non-null for "WA", "CO", "DC" (spot checks)
    - Verify all schemas have valid JSON in fields property
  </action>
  <verify>
    <automated>cd /Users/roybomb/Desktop/RealEstateHunter && npx vitest run src/services/disclosures/form-schema.test.ts --reporter=verbose 2>&1 | tail -20 && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All 50 states + DC have disclosure form schemas, state info entries, and appear in constants. LAUNCH_STATES preserved for backward compat. ALL_STATES added. Build compiles. Tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsx -e "import { ALL_STATE_CONFIGS } from './src/workflow/states/index.ts'; console.log(Object.keys(ALL_STATE_CONFIGS).length);"` outputs 51
- `npx vitest run src/services/disclosures/form-schema.test.ts` — all pass
- `npx next build 2>&1 | tail -20` — build succeeds
- grep confirms ALL_STATES in constants.ts has 51 entries
</verification>

<success_criteria>
- 51 state workflow configs exist (50 states + DC)
- 51 disclosure form schemas exist
- 51 state info entries exist
- getStateWorkflowConfig, getFormSchemaForState, and getStateInfo work for ANY US state code
- Existing 10-state code is not broken (LAUNCH_STATES backward compat)
- Build compiles, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-mls-direct-integration-scale/06-02-SUMMARY.md`
</output>
