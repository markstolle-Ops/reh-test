---
phase: 04-transaction-engine-state-compliance
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/ai/agents/transaction-coordinator.ts
  - src/ai/prompts/transaction-coordinator.ts
  - src/inngest/functions/track-transaction-deadlines.ts
  - src/inngest/functions/track-transaction-deadlines.test.ts
  - src/inngest/functions/cfpb-disclosure-monitor.ts
  - src/inngest/functions/cfpb-disclosure-monitor.test.ts
  - src/app/api/inngest/route.ts
  - src/app/buyer/transactions/[id]/page.tsx
  - src/app/seller/transactions/[id]/page.tsx
autonomous: true
requirements: [TXCO-01, TXCO-03, TXCO-04, TXCO-05]

must_haves:
  truths:
    - "AI generates state-specific transaction checklist after offer acceptance"
    - "Inngest function schedules deadline reminders at 48h, 24h, and 0h before each deadline"
    - "CFPB 3-day closing disclosure monitor alerts when CD not sent in time"
    - "AI flags missing required documents per state by comparing config requiredDocuments against transaction events"
    - "Transaction dashboard shows real-time status of all pending items for buyer and seller"
  artifacts:
    - path: "src/inngest/functions/track-transaction-deadlines.ts"
      provides: "Deadline scheduling Inngest function"
      exports: ["trackTransactionDeadlines"]
    - path: "src/inngest/functions/cfpb-disclosure-monitor.ts"
      provides: "CFPB TRID compliance monitor"
      exports: ["cfpbDisclosureMonitor"]
    - path: "src/app/buyer/transactions/[id]/page.tsx"
      provides: "Buyer transaction dashboard"
    - path: "src/app/seller/transactions/[id]/page.tsx"
      provides: "Seller transaction dashboard"
  key_links:
    - from: "src/inngest/functions/track-transaction-deadlines.ts"
      to: "src/db/schema.ts"
      via: "insert into transactionDeadlines"
      pattern: "db\\.insert\\(transactionDeadlines\\)"
    - from: "src/inngest/functions/cfpb-disclosure-monitor.ts"
      to: "src/services/transaction/deadlines.ts"
      via: "calculateClosingDisclosureDeadline"
      pattern: "import.*calculateClosingDisclosureDeadline"
---

<objective>
Build the AI transaction coordinator with deadline tracking, CFPB compliance monitoring, document compliance checks, and transaction dashboard UI.

Purpose: After offer acceptance, the platform must track deadlines, monitor CFPB 3-day closing disclosure compliance, flag missing documents, and show real-time status to both buyer and seller.

Output: Inngest deadline/CFPB functions, AI coordinator agent, transaction dashboard pages, passing tests.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-engine-state-compliance/04-RESEARCH.md
@.planning/phases/04-transaction-engine-state-compliance/04-01-SUMMARY.md
@.planning/phases/04-transaction-engine-state-compliance/04-02-SUMMARY.md

@src/inngest/client.ts
@src/services/transaction/deadlines.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inngest deadline tracker + CFPB disclosure monitor</name>
  <files>src/inngest/functions/track-transaction-deadlines.ts, src/inngest/functions/track-transaction-deadlines.test.ts, src/inngest/functions/cfpb-disclosure-monitor.ts, src/inngest/functions/cfpb-disclosure-monitor.test.ts, src/ai/agents/transaction-coordinator.ts, src/ai/prompts/transaction-coordinator.ts, src/app/api/inngest/route.ts</files>
  <behavior>
    - Test: trackTransactionDeadlinesRaw inserts deadline rows from config steps with deadlineDays
    - Test: cfpbDisclosureMonitorRaw checks for closing_disclosure_sent event; returns alert=true when missing and past mustSendBy
    - Test: cfpbDisclosureMonitorRaw returns alert=false when closing_disclosure_sent event exists
    - Test: CFPB monitor only applies to mortgage-financed transactions (not cash)
    - Test: generateChecklist returns array of step labels + required documents from state config
  </behavior>
  <action>
    1. Create `src/inngest/functions/track-transaction-deadlines.ts`:
       - Export raw function `trackTransactionDeadlinesRaw({ transactionId, config, offerAcceptedDate })` for testability
       - Reads config.steps, for each step with deadlineDays, inserts row into transactionDeadlines: { transactionId, deadlineType: step.deadlineType, dueAt: addBusinessDays(offerAcceptedDate, step.deadlineDays) }
       - Wrap with `inngest.createFunction({ id: 'track-transaction-deadlines' }, { event: 'transaction/deadlines.schedule' }, handler)`
       - Handler uses step.run() for DB operations, step.sleep() for 48h/24h/0h reminder intervals, Resend email for reminders
       - Reminder emails contain ONLY deadline info + deep link to transaction dashboard — NO wire instructions

    2. Create `src/inngest/functions/cfpb-disclosure-monitor.ts`:
       - Export raw function `cfpbDisclosureMonitorRaw({ transactionId, closingDate, loanType })` for testability
       - If loanType === 'cash', skip TRID enforcement (return early)
       - Calculate mustSendBy using calculateClosingDisclosureDeadline from 04-02
       - Query transactionEvents for eventType='closing_disclosure_sent'
       - If not found and today > mustSendBy, send alert email via Resend to both parties
       - Wrap with inngest.createFunction({ id: 'cfpb-disclosure-monitor' }, { event: 'transaction/closing.scheduled' })

    3. Create `src/ai/agents/transaction-coordinator.ts`:
       - Export `generateChecklist(config: StateWorkflowConfig)` — maps config.steps to user-facing checklist array
       - Export `checkDocumentCompliance(config, transactionEvents)` — compares required documents per step against existing signatureEnvelope/event records, returns { complete: string[], missing: string[] }
       - Export `formatChecklistWithStatus(checklist, compliance)` for dashboard rendering

    4. Create `src/ai/prompts/transaction-coordinator.ts` — system prompt for AI coordinator (deadline summaries, document status, UPL disclaimer)

    5. Register new Inngest functions in `src/app/api/inngest/route.ts` (add to existing serve() array)

    Write tests FIRST for raw functions. Mock db and Inngest step.
  </action>
  <verify>
    <automated>npx vitest run src/inngest/functions/track-transaction-deadlines.test.ts src/inngest/functions/cfpb-disclosure-monitor.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Deadline tracker inserts correct rows from state config. CFPB monitor alerts when CD not sent in time, skips cash transactions. Checklist generator maps config steps to user-facing list.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction dashboard pages (buyer + seller views)</name>
  <files>src/app/buyer/transactions/[id]/page.tsx, src/app/seller/transactions/[id]/page.tsx</files>
  <action>
    1. Create `src/app/buyer/transactions/[id]/page.tsx` (Server Component):
       - Requires auth via `auth()` — redirect to /sign-in if not authenticated
       - Fetch transaction by ID (GET /api/transactions/[id] or direct DB query)
       - Verify current user is the buyer on this transaction (403 otherwise)
       - Load state workflow config via getStateWorkflowConfig(transaction.propertyState)
       - Display: transaction status badge, listing summary (address, price), state-specific checklist with completion status
       - Show deadline timeline: list all transactionDeadlines with dueAt, status (pending/complete/overdue)
       - Show missing documents section from checkDocumentCompliance
       - Show event history (timeline of all transactionEvents ordered by occurredAt)
       - Include placeholder slot for wire instructions link (wired in Plan 04-06)
       - Use Tailwind for styling — consistent with existing buyer dashboard

    2. Create `src/app/seller/transactions/[id]/page.tsx`:
       - Same structure as buyer but with seller-specific view
       - Verify current user is the seller on this transaction
       - Show incoming offers section if status is offer_submitted
       - Show accept/reject/counter buttons for active offers
       - Same deadline timeline and document compliance sections

    Both pages read from transactions + transactionEvents + transactionDeadlines tables. No client-side state machine — dashboard reads the cached currentStatus from DB.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Buyer and seller transaction dashboards render with status, checklist, deadline timeline, document compliance, and event history. Build succeeds.</done>
</task>

</tasks>

<verification>
npx vitest run src/inngest/functions/track-transaction-deadlines.test.ts src/inngest/functions/cfpb-disclosure-monitor.test.ts 2>&1 | tail -20
npx next build 2>&1 | tail -20
</verification>

<success_criteria>
- Inngest deadline tracker creates correct deadline rows from state config
- CFPB monitor alerts on missing closing disclosure (mortgage only)
- Transaction dashboard shows real-time status for buyer and seller
- Checklist reflects state-specific steps and document requirements
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-engine-state-compliance/04-04-SUMMARY.md`
</output>
