---
phase: 04-transaction-engine-state-compliance
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/ai/agents/transaction-guide.ts
  - src/ai/agents/transaction-guide.test.ts
  - src/ai/prompts/transaction-guide.ts
  - src/app/api/transaction-guide/route.ts
  - src/components/transaction/StateRequirementsNotice.tsx
  - src/components/transaction/StateRequirementsNotice.test.ts
autonomous: true
requirements: [LEGL-07, NEGO-03]

must_haves:
  truths:
    - "Platform displays state-specific legal requirements to users before they begin a transaction"
    - "AI guides buyer through offer/counteroffer process with templates"
    - "Transaction guide RAG agent answers state-specific process questions with UPL guardrails"
  artifacts:
    - path: "src/components/transaction/StateRequirementsNotice.tsx"
      provides: "State requirements banner shown before transaction starts"
      exports: ["StateRequirementsNotice"]
    - path: "src/ai/agents/transaction-guide.ts"
      provides: "RAG-grounded state-law Q&A agent"
      exports: ["streamTransactionGuide"]
    - path: "src/app/api/transaction-guide/route.ts"
      provides: "POST streaming endpoint for transaction guide chat"
      exports: ["POST"]
  key_links:
    - from: "src/components/transaction/StateRequirementsNotice.tsx"
      to: "src/workflow/states/index.ts"
      via: "getStateWorkflowConfig for legalRequirementsSummary"
      pattern: "import.*getStateWorkflowConfig"
    - from: "src/ai/agents/transaction-guide.ts"
      to: "src/db/schema.ts"
      via: "pgvector knowledge chunks query"
      pattern: "knowledgeChunks"
---

<objective>
Build the Transaction Guide AI agent (RAG-grounded state-law Q&A) and StateRequirementsNotice component.

Purpose: Users must see state-specific legal requirements before starting a transaction (LEGL-07). The Transaction Guide provides ongoing AI-powered process guidance with offer/counteroffer templates (NEGO-03), grounded in the existing pgvector knowledge base with strict UPL guardrails.

Output: StateRequirementsNotice component, Transaction Guide AI agent, streaming API route, tests.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-engine-state-compliance/04-RESEARCH.md
@.planning/phases/04-transaction-engine-state-compliance/04-01-SUMMARY.md

@src/workflow/types.ts
@src/workflow/states/index.ts
@src/ai/prompts/upl-disclaimer.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StateRequirementsNotice component + unit test</name>
  <files>src/components/transaction/StateRequirementsNotice.tsx, src/components/transaction/StateRequirementsNotice.test.ts</files>
  <behavior>
    - Test: getRequirementsNotice('GA') returns legalRequirementsSummary containing 'attorney'
    - Test: getRequirementsNotice('CA') returns legalRequirementsSummary NOT containing 'attorney'
    - Test: getRequirementsNotice('NY') returns notice mentioning 'partner broker'
    - Test: all 10 launch states return non-empty legalRequirementsSummary
  </behavior>
  <action>
    1. Create `src/components/transaction/StateRequirementsNotice.tsx`:
       - Export `getRequirementsNotice(stateCode: string)` utility — calls getStateWorkflowConfig and returns { summary: string, closingType, attorneyRequired: boolean, brokerRequired: boolean }
       - Export `StateRequirementsNotice` React component (Server Component) accepting `stateCode` prop
       - Renders a prominent banner (yellow/amber background) with:
         - State name + closing type label
         - legalRequirementsSummary text
         - If attorneyReferralRequired: "You will be connected with an attorney referral"
         - If partnerBrokerRequired (NY): "A licensed broker is required for this transaction"
         - "By proceeding, you acknowledge these state-specific requirements"
       - Use Tailwind classes consistent with existing component styling

    2. Write tests for `getRequirementsNotice` utility (exported pure function — testable without DOM). Follow existing project pattern of testing utilities, not rendered DOM (vitest environment: node).
  </action>
  <verify>
    <automated>npx vitest run src/components/transaction/StateRequirementsNotice.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>StateRequirementsNotice renders state-specific legal requirements. GA/NC show attorney required. NY shows broker required. All 10 states have non-empty summaries.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction Guide RAG agent + offer template tool</name>
  <files>src/ai/agents/transaction-guide.ts, src/ai/agents/transaction-guide.test.ts, src/ai/prompts/transaction-guide.ts, src/app/api/transaction-guide/route.ts</files>
  <action>
    1. Create `src/ai/prompts/transaction-guide.ts`:
       - System prompt: "You are a transaction guide helping users understand the real estate transaction process for their specific state. You explain steps, timelines, and required documents. You can generate offer and counteroffer letter templates. You do NOT provide legal advice, interpret contracts, or advise on legal rights. Direct all legal questions to a licensed attorney."
       - Append UPL_DISCLAIMER
       - Include state-specific context injection point (populated at runtime from state config)

    2. Create `src/ai/agents/transaction-guide.ts`:
       - Export `streamTransactionGuide({ stateCode, transactionId?, userMessage, userRole })` using AI SDK v6 streamText()
       - Query existing pgvector knowledge_chunks for relevant state-law context (same RAG pattern as Phase 2 chatbot)
       - Include `generateOfferTemplate` tool: inputSchema z.object({ offerPriceCents, buyerName, propertyAddress, contingencies: z.array(z.string()), closingDatePreference }), execute returns formatted offer letter template string (NEGO-03)
       - Include `generateCounterTemplate` tool: similar but for counteroffer letters
       - Both templates include prominent "This is a template only — not a legal document. Have an attorney review before submitting." disclaimer
       - Model: openai('gpt-4o')

    3. Create `src/app/api/transaction-guide/route.ts`:
       - POST route, requires auth
       - Accepts { stateCode, transactionId?, message, role }
       - Calls streamTransactionGuide
       - Returns streaming response

    4. Write test asserting: system prompt contains UPL disclaimer, generateOfferTemplate tool has correct inputSchema shape, state config context is injected.
  </action>
  <verify>
    <automated>npx vitest run src/ai/agents/transaction-guide.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Transaction Guide streams state-specific process guidance. Offer/counteroffer template tools produce formatted letters with legal disclaimers. UPL guardrails enforced in every response.</done>
</task>

</tasks>

<verification>
npx vitest run src/components/transaction src/ai/agents/transaction-guide 2>&1 | tail -20
npx next build 2>&1 | tail -20
</verification>

<success_criteria>
- StateRequirementsNotice renders correct info for all 10 launch states
- Transaction Guide RAG agent uses pgvector knowledge base
- Offer/counteroffer template tools produce structured output
- All UPL disclaimers present
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-engine-state-compliance/04-05-SUMMARY.md`
</output>
