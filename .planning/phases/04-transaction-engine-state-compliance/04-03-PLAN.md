---
phase: 04-transaction-engine-state-compliance
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/services/negotiation/comps.ts
  - src/services/negotiation/comps.test.ts
  - src/ai/agents/negotiation.ts
  - src/ai/agents/negotiation.test.ts
  - src/ai/prompts/negotiation-buyer.ts
  - src/ai/prompts/negotiation-seller.ts
  - src/app/api/negotiation/comps/route.ts
  - src/app/api/negotiation/strategy/route.ts
autonomous: true
requirements: [NEGO-01, NEGO-02, NEGO-03, NEGO-04, NEGO-05]

must_haves:
  truths:
    - "Buyer can view comparable sales analysis for any listing"
    - "AI suggests offer price strategy with comps context"
    - "Seller receives counteroffer strategy recommendations"
    - "Every AI negotiation response includes UPL disclaimer"
    - "AI never interprets contracts or provides legal guidance"
  artifacts:
    - path: "src/services/negotiation/comps.ts"
      provides: "Comparable sales data fetcher (mls_listings fallback)"
      exports: ["fetchComps"]
    - path: "src/ai/agents/negotiation.ts"
      provides: "AI negotiation assistant (buyer + seller modes)"
      exports: ["streamNegotiationGuidance"]
    - path: "src/app/api/negotiation/comps/route.ts"
      provides: "GET comps endpoint"
      exports: ["GET"]
    - path: "src/app/api/negotiation/strategy/route.ts"
      provides: "POST streaming negotiation strategy endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/ai/agents/negotiation.ts"
      to: "src/services/negotiation/comps.ts"
      via: "fetchComps for context injection"
      pattern: "import.*fetchComps"
    - from: "src/app/api/negotiation/strategy/route.ts"
      to: "src/ai/agents/negotiation.ts"
      via: "streamNegotiationGuidance call"
      pattern: "import.*streamNegotiationGuidance"
---

<objective>
Build the AI negotiation assistant with comparable sales analysis for both buyers and sellers.

Purpose: Buyers need data-driven offer strategy. Sellers need counteroffer recommendations. The comps service provides market context; the AI agent generates strategy with explicit UPL disclaimers.

Output: Comps service (mls_listings fallback), AI negotiation agent, buyer/seller prompt variants, API routes, tests.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-engine-state-compliance/04-RESEARCH.md

@src/db/schema.ts
@src/ai/prompts/upl-disclaimer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Comps service + AI negotiation agent with UPL guardrails</name>
  <files>src/services/negotiation/comps.ts, src/services/negotiation/comps.test.ts, src/ai/agents/negotiation.ts, src/ai/agents/negotiation.test.ts, src/ai/prompts/negotiation-buyer.ts, src/ai/prompts/negotiation-seller.ts</files>
  <behavior>
    - Test: fetchComps returns array of { soldPriceCents, sqft, daysAgo, address } for a given zip code from mls_listings table
    - Test: fetchComps returns empty array when no sold listings in zip
    - Test: negotiation agent system prompt ALWAYS contains UPL disclaimer text
    - Test: negotiation agent system prompt for buyer role contains "buyer" role context
    - Test: negotiation agent system prompt for seller role contains "seller" / "counteroffer" context
    - Test: suggestOfferPrice tool has inputSchema with suggestedOfferCents, rationale, confidenceLevel
  </behavior>
  <action>
    1. Create `src/services/negotiation/comps.ts`:
       - Export `fetchComps({ zip, propertyType?, limit? })` that queries `mls_listings` table for status='sold' within same zip code, ordered by soldDate DESC, limit 10
       - Returns array of { soldPriceCents, sqft, soldDate, address, daysAgo (computed) }
       - This is the MVP fallback — ATTOM Data API integration is deferred until vendor contract is established (per research open question)
       - Add a `// TODO: Replace with ATTOM Data API when contract is established` comment

    2. Create `src/ai/prompts/negotiation-buyer.ts`:
       - Export buyer system prompt: "You are an AI negotiation assistant helping a buyer evaluate their offer strategy based on recent comparable sales. You provide pricing analysis and general market context only. You do NOT interpret contracts, advise on legal rights, or provide legal guidance of any kind. Direct all legal questions to a licensed attorney."
       - Append UPL_DISCLAIMER from existing `@/ai/prompts/upl-disclaimer.ts`

    3. Create `src/ai/prompts/negotiation-seller.ts`:
       - Export seller system prompt: similar but for seller counteroffer strategy

    4. Create `src/ai/agents/negotiation.ts`:
       - Export `streamNegotiationGuidance({ comps, listingPrice, daysOnMarket, userRole, userMessage })` using AI SDK v6 `streamText()` pattern
       - Use `openai('gpt-4o')` model
       - Include `suggestOfferPrice` tool with `inputSchema` (z.object with suggestedOfferCents, rationale, confidenceLevel)
       - System prompt selects buyer vs seller variant based on userRole
       - Comps context injected into system prompt as formatted text
       - Use `toUIMessageStreamResponse()` for streaming response (same pattern as Phase 2 chatbot)

    Write tests FIRST. Comps test mocks db. Negotiation test asserts prompt structure and tool schema without calling OpenAI.
  </action>
  <verify>
    <automated>npx vitest run src/services/negotiation src/ai/agents/negotiation 2>&1 | tail -20</automated>
  </verify>
  <done>Comps service queries mls_listings for sold properties. AI agent produces streaming negotiation guidance with UPL disclaimer in every response. Buyer and seller prompt variants are distinct.</done>
</task>

<task type="auto">
  <name>Task 2: Negotiation API routes (comps + strategy)</name>
  <files>src/app/api/negotiation/comps/route.ts, src/app/api/negotiation/strategy/route.ts</files>
  <action>
    1. `GET /api/negotiation/comps?listingId={id}` route:
       - Requires auth
       - Looks up listing to get zip code and property type
       - Calls fetchComps({ zip, propertyType })
       - Returns JSON array of comps
       - Returns 404 if listing not found

    2. `POST /api/negotiation/strategy` route:
       - Requires auth
       - Accepts JSON body: { listingId, message, role: 'buyer' | 'seller' }
       - Fetches comps for the listing
       - Looks up listing price and daysOnMarket (computed from listing createdAt)
       - Calls streamNegotiationGuidance with all context
       - Returns streaming response via toUIMessageStreamResponse()
       - Include `X-UPL-Disclaimer: true` header on response

    Follow existing API patterns: auth(), error handling, JSON responses.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Comps endpoint returns comparable sales for any listing. Strategy endpoint streams AI negotiation guidance with role-appropriate prompts.</done>
</task>

</tasks>

<verification>
npx vitest run src/services/negotiation src/ai/agents/negotiation 2>&1 | tail -20
npx next build 2>&1 | tail -20
</verification>

<success_criteria>
- Comps service returns sold listings from same zip
- AI agent includes UPL disclaimer in every system prompt
- Buyer and seller get role-appropriate guidance
- suggestOfferPrice tool returns structured JSON
- API routes compile and stream correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-engine-state-compliance/04-03-SUMMARY.md`
</output>
