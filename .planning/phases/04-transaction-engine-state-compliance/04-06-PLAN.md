---
phase: 04-transaction-engine-state-compliance
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-04]
files_modified:
  - src/db/schema.ts
  - src/app/api/wire-instructions/[transactionId]/route.ts
  - src/app/buyer/transactions/[id]/wire-instructions/page.tsx
  - src/services/transaction/wire-instructions.ts
  - src/services/transaction/wire-instructions.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Wire instructions are displayed only via authenticated in-app interface"
    - "Wire instructions page requires MFA verification (Clerk step-up or custom gate)"
    - "Wire instructions are NEVER transmitted via email"
    - "Every page load logs wire_instructions_viewed event to transaction_events"
    - "Prominent fraud warning banner is visible on wire instructions page"
  artifacts:
    - path: "src/app/buyer/transactions/[id]/wire-instructions/page.tsx"
      provides: "Authenticated MFA-gated wire instructions display"
    - path: "src/app/api/wire-instructions/[transactionId]/route.ts"
      provides: "GET wire instructions (auth + MFA required)"
      exports: ["GET"]
    - path: "src/services/transaction/wire-instructions.ts"
      provides: "Wire instruction storage and retrieval service"
      exports: ["getWireInstructions", "setWireInstructions"]
  key_links:
    - from: "src/app/buyer/transactions/[id]/wire-instructions/page.tsx"
      to: "src/app/api/wire-instructions/[transactionId]/route.ts"
      via: "fetch wire instructions after MFA check"
      pattern: "wire-instructions"
    - from: "src/app/api/wire-instructions/[transactionId]/route.ts"
      to: "src/services/transaction/events.ts"
      via: "appendTransactionEvent for audit trail"
      pattern: "appendTransactionEvent.*wire_instructions_viewed"
---

<objective>
Build the wire fraud security layer with MFA-gated in-app wire instruction display.

Purpose: Wire fraud is the most dangerous financial risk in real estate. All wiring instructions must be displayed ONLY via an authenticated, MFA-gated in-app interface. Never via email. Every access is logged for audit.

Output: Wire instructions DB table, authenticated API route, MFA-gated display page, fraud warning banner, audit trail logging.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-engine-state-compliance/04-RESEARCH.md
@.planning/phases/04-transaction-engine-state-compliance/04-02-SUMMARY.md

@src/db/schema.ts
@src/services/transaction/events.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire instructions schema + service layer</name>
  <files>src/db/schema.ts, src/services/transaction/wire-instructions.ts, src/services/transaction/wire-instructions.test.ts</files>
  <behavior>
    - Test: setWireInstructions stores encrypted data (or at minimum, stores data to DB)
    - Test: getWireInstructions returns decrypted wire data for valid transaction
    - Test: getWireInstructions returns null for nonexistent transaction
    - Test: accessing wire instructions logs wire_instructions_viewed event
  </behavior>
  <action>
    1. Add `wireInstructions` table to `src/db/schema.ts`:
       - id (text PK), transactionId (text FK unique), bankName (text), routingNumber (text), accountNumber (text), accountName (text), referenceNote (text nullable), setByUserId (text), createdAt (timestamp defaultNow), updatedAt (timestamp defaultNow)
       - NOTE: For MVP, store as plaintext in DB. Add comment: "// TODO: Add column-level encryption via Supabase Vault (pgsodium) before production"
       - Add relation to transactions table

    2. Create `src/services/transaction/wire-instructions.ts`:
       - Export `setWireInstructions({ transactionId, bankName, routingNumber, accountNumber, accountName, referenceNote, setByUserId })` — upserts wire instruction row
       - Export `getWireInstructions(transactionId, viewerUserId)` — fetches wire instructions AND logs wire_instructions_viewed event to transactionEvents via appendTransactionEvent
       - Verify viewer is buyer or seller on the transaction before returning data

    Write tests mocking db.
  </action>
  <verify>
    <automated>npx vitest run src/services/transaction/wire-instructions.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Wire instructions are stored per-transaction. Every retrieval logs an audit event. Authorization check prevents unauthorized access.</done>
</task>

<task type="auto">
  <name>Task 2: MFA-gated wire instructions page + API route</name>
  <files>src/app/api/wire-instructions/[transactionId]/route.ts, src/app/buyer/transactions/[id]/wire-instructions/page.tsx</files>
  <action>
    1. Create `GET /api/wire-instructions/[transactionId]` route:
       - Requires auth via `auth()`
       - Check if user has MFA enabled via Clerk — if user.twoFactorEnabled is false, return 403 with message "MFA required to view wire instructions. Enable two-factor authentication in your account settings."
       - If MFA enabled, call getWireInstructions(transactionId, userId)
       - Return wire instruction data (bankName, routingNumber masked except last 4, accountNumber masked except last 4, accountName, referenceNote)
       - Full unmasked data available only via a second confirmation step (click "Reveal full numbers" which re-fetches with ?reveal=true param)

    2. Create `src/app/buyer/transactions/[id]/wire-instructions/page.tsx`:
       - Server Component with auth check
       - If user has no MFA: show prominent message requiring MFA setup with link to Clerk user settings
       - If MFA enabled: fetch and display wire instructions
       - PROMINENT fraud warning banner at top (red/alert background):
         "WIRE FRAUD WARNING: These wire instructions are the ONLY authoritative source. If you received an email, text, or phone call with DIFFERENT wire instructions, DO NOT send money. Call your title company directly using a verified phone number."
       - Display: bankName, masked routing/account numbers, "Reveal full numbers" button
       - Show last-accessed timestamp from audit trail
       - No print/copy buttons (minimize screenshot risk) — but don't prevent browser print (security theater)
       - Link back to transaction dashboard

    3. Update the buyer transaction dashboard page (from 04-04) to include a "View Wire Instructions" link that navigates to this page — only visible when transaction status is pending_closing or later.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Wire instructions page requires MFA, shows fraud warning, masks sensitive numbers by default, logs every view. No wire data in emails.</done>
</task>

</tasks>

<verification>
npx vitest run src/services/transaction/wire-instructions 2>&1 | tail -20
npx next build 2>&1 | tail -20
</verification>

<success_criteria>
- Wire instructions stored in DB with audit trail on every access
- MFA required to view wire instructions
- Fraud warning banner prominently displayed
- Wire data never in email templates
- Routing/account numbers masked by default
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-engine-state-compliance/04-06-SUMMARY.md`
</output>
