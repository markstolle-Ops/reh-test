---
phase: 04-transaction-engine-state-compliance
plan: 07
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - src/services/mls/syndication.ts
  - src/services/mls/syndication.test.ts
  - src/inngest/functions/syndicate-to-mls.ts
  - src/inngest/functions/syndicate-to-mls.test.ts
  - src/app/api/inngest/route.ts
autonomous: true
requirements: [MLS-01]

must_haves:
  truths:
    - "Seller listings can be submitted to MLS via broker partner network"
    - "MLS syndication is triggered asynchronously (Inngest) after listing publish"
    - "Syndication status is trackable per listing"
  artifacts:
    - path: "src/services/mls/syndication.ts"
      provides: "Upgraded MLS syndication service (real vendor call or structured email)"
      exports: ["submitToMls", "getMlsStatus"]
    - path: "src/inngest/functions/syndicate-to-mls.ts"
      provides: "Inngest function that triggers MLS submission"
      exports: ["syndicateToMls"]
  key_links:
    - from: "src/inngest/functions/syndicate-to-mls.ts"
      to: "src/services/mls/syndication.ts"
      via: "submitToMls call"
      pattern: "import.*submitToMls"
---

<objective>
Upgrade the MLS syndication stub to a real broker partner submission workflow.

Purpose: Seller listings must reach buyers on MLS (MLS-01). The existing stub in src/services/mls/syndication.ts needs to be upgraded to send a structured submission to a flat-fee MLS broker partner (ListWithFreedom or Homecoin). Since no public REST API exists for these vendors, the MVP implementation sends a structured email submission via Resend.

Output: Upgraded syndication service, Inngest async function, syndication status tracking, tests.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-engine-state-compliance/04-RESEARCH.md

@src/services/mls/syndication.ts
@src/inngest/client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade MLS syndication service + Inngest async trigger</name>
  <files>src/services/mls/syndication.ts, src/services/mls/syndication.test.ts, src/inngest/functions/syndicate-to-mls.ts, src/inngest/functions/syndicate-to-mls.test.ts, src/app/api/inngest/route.ts</files>
  <behavior>
    - Test: submitToMls sends structured email via Resend with listing details (address, price, beds, baths, sqft, photos, MLS data sheet)
    - Test: submitToMls returns { submissionId, status: 'submitted', submittedAt }
    - Test: getMlsStatus returns current syndication status for a listing
    - Test: syndicateToMlsRaw calls submitToMls with correct listing data
  </behavior>
  <action>
    1. Upgrade `src/services/mls/syndication.ts`:
       - Keep existing `MlsSyndicationRequest` and `MlsSyndicationStatus` interfaces
       - Replace stub implementation of `submitToMls()` with real email submission:
         - Build structured HTML email with all listing details: address, price, beds, baths, sqft, lot size, property type, description, photo URLs, seller contact info
         - Send via Resend to a configurable `MLS_BROKER_EMAIL` env var (default: placeholder — must be set before production)
         - Store submission record: add `mlsSyndicationStatus` field to listings table (or a new `mls_syndications` table with listingId, submissionId, status, submittedAt, confirmedAt)
         - Return { submissionId: nanoid(), status: 'submitted', submittedAt: new Date() }
       - Update `getMlsStatus()` to read from DB instead of returning stub

    2. Create `src/inngest/functions/syndicate-to-mls.ts`:
       - Export raw function `syndicateToMlsRaw(listingId)` for testability
       - Fetches listing from DB, calls submitToMls with listing data
       - Wrap with `inngest.createFunction({ id: 'syndicate-to-mls' }, { event: 'listing/published' })`
       - This event should already be fired when a listing status changes to 'active' (from Phase 2 — check and wire if needed)

    3. Add new `mls_syndications` table to schema if needed: id, listingId (FK), submissionId, status (submitted/confirmed/rejected), brokerEmail, submittedAt, confirmedAt, rejectionReason

    4. Register new Inngest function in `src/app/api/inngest/route.ts`

    5. Add `MLS_BROKER_EMAIL` to `.env.example` with comment: "# Email for flat-fee MLS broker partner (e.g., ListWithFreedom) — required for MLS syndication"

    Write tests FIRST. Mock Resend and db.
  </action>
  <verify>
    <automated>npx vitest run src/services/mls/syndication.test.ts src/inngest/functions/syndicate-to-mls.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>MLS syndication sends structured email to broker partner. Submission status tracked in DB. Inngest triggers syndication asynchronously on listing publish. $299 fee already visible from Phase 2.</done>
</task>

</tasks>

<verification>
npx vitest run src/services/mls src/inngest/functions/syndicate-to-mls 2>&1 | tail -20
npx next build 2>&1 | tail -20
</verification>

<success_criteria>
- submitToMls sends real email via Resend (not stub)
- Syndication status persisted and queryable
- Inngest function fires on listing publish
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-engine-state-compliance/04-07-SUMMARY.md`
</output>
