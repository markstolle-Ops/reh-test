---
phase: 04-transaction-engine-state-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow/types.ts
  - src/workflow/engine.ts
  - src/workflow/engine.test.ts
  - src/workflow/states/CA.ts
  - src/workflow/states/TX.ts
  - src/workflow/states/FL.ts
  - src/workflow/states/NY.ts
  - src/workflow/states/GA.ts
  - src/workflow/states/NC.ts
  - src/workflow/states/AZ.ts
  - src/workflow/states/OH.ts
  - src/workflow/states/PA.ts
  - src/workflow/states/IL.ts
  - src/workflow/states/index.ts
autonomous: true
requirements: [LEGL-01, LEGL-02, LEGL-03, LEGL-04, LEGL-05, LEGL-06]

must_haves:
  truths:
    - "Each of the 10 launch states has a data-driven workflow config file"
    - "XState machine routes attorney-required states (GA, NC) through attorney_review step"
    - "XState machine routes customary-attorney states (NY, IL) through optional attorney step"
    - "XState machine routes title-company states (CA, TX, FL, AZ, OH, PA) through self-service FSBO path"
    - "State configs can be swapped without changing machine code"
    - "NY config includes partnerBrokerRequired: true per STATE.md decision"
  artifacts:
    - path: "src/workflow/types.ts"
      provides: "StateWorkflowConfig and WorkflowStep interfaces"
      exports: ["StateWorkflowConfig", "WorkflowStep"]
    - path: "src/workflow/engine.ts"
      provides: "XState 5 transaction machine factory"
      exports: ["transactionMachine", "createTransactionActor"]
    - path: "src/workflow/states/index.ts"
      provides: "Registry of all 10 state configs"
      exports: ["getStateWorkflowConfig", "ALL_STATE_CONFIGS"]
  key_links:
    - from: "src/workflow/engine.ts"
      to: "src/workflow/types.ts"
      via: "StateWorkflowConfig input type"
      pattern: "import.*StateWorkflowConfig.*from.*types"
    - from: "src/workflow/states/*.ts"
      to: "src/workflow/types.ts"
      via: "satisfies StateWorkflowConfig"
      pattern: "satisfies StateWorkflowConfig"
---

<objective>
Build the XState 5 workflow engine and 10 per-state config files that drive the entire transaction lifecycle.

Purpose: The XState machine is the central nervous system of Phase 4. Every other system (transaction service, AI agents, deadline tracking) triggers from or reacts to state machine transitions. This must be built first.

Output: XState 5 machine factory, 10 state workflow configs, type definitions, passing unit tests.
</objective>

<execution_context>
@/Users/roybomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/roybomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transaction-engine-state-compliance/04-RESEARCH.md

@src/lib/states.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define workflow types and 10 state config files</name>
  <files>src/workflow/types.ts, src/workflow/states/CA.ts, src/workflow/states/TX.ts, src/workflow/states/FL.ts, src/workflow/states/NY.ts, src/workflow/states/GA.ts, src/workflow/states/NC.ts, src/workflow/states/AZ.ts, src/workflow/states/OH.ts, src/workflow/states/PA.ts, src/workflow/states/IL.ts, src/workflow/states/index.ts</files>
  <action>
    1. Create `src/workflow/types.ts` with exported interfaces:
       - `WorkflowStep`: { id: string, label: string, requiredDocuments: string[], deadlineDays?: number, deadlineType?: 'inspection' | 'financing' | 'closing_disclosure' | 'closing', triggers?: string[] }
       - `StateWorkflowConfig`: { stateCode: string, stateName: string, closingType: ClosingType (import from @/types), fsboAllowed: boolean, ronAvailable: boolean, steps: WorkflowStep[], legalRequirementsSummary: string, attorneyReferralRequired: boolean, partnerBrokerRequired: boolean }
       - Import `ClosingType` from `@/types` (already exists: 'title-company' | 'attorney-required' | 'customary-attorney')

    2. Create one config file per state in `src/workflow/states/`. Each file exports a `const xxxConfig: StateWorkflowConfig` (using `satisfies`). Classification:
       - Title-company FSBO (CA, TX, FL, AZ, OH, PA): closingType='title-company', fsboAllowed=true, attorneyReferralRequired=false, partnerBrokerRequired=false. Steps: offer_submitted -> offer_accepted -> inspection_period -> pending_closing -> closed
       - Attorney-required (GA, NC): closingType='attorney-required', fsboAllowed=true, attorneyReferralRequired=true. Steps include attorney_review after offer_accepted, triggers=['transaction/attorney.notify']
       - Customary-attorney (NY, IL): closingType='customary-attorney', attorneyReferralRequired=true (attorney fee applies per Phase 1 decision). NY MUST have partnerBrokerRequired=true (HIGH risk per STATE.md). IL has partnerBrokerRequired=false.
       - Each config must include realistic `legalRequirementsSummary` text (1-3 sentences about state-specific requirements)
       - Each config must include realistic `requiredDocuments` arrays per step (purchase_agreement, inspection_report, closing_disclosure, title_commitment, signed_deed, settlement_statement, attorney_assignment where applicable)
       - Include `deadlineDays` on inspection (10 days), financing (21 days), closing_disclosure (3 days from closing), closing (30 days from offer)

    3. Create `src/workflow/states/index.ts`:
       - Import all 10 configs
       - Export `ALL_STATE_CONFIGS: Record<string, StateWorkflowConfig>` mapping state codes to configs
       - Export `getStateWorkflowConfig(stateCode: string): StateWorkflowConfig` that returns config or throws for unknown state
  </action>
  <verify>
    <automated>npx vitest run src/workflow/states 2>&1 | tail -20</automated>
  </verify>
  <done>10 state config files exist with correct closingType, steps, requiredDocuments, and legalRequirementsSummary. NY has partnerBrokerRequired=true. getStateWorkflowConfig returns correct config for each state code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build XState 5 transaction machine with guard-driven routing</name>
  <files>src/workflow/engine.ts, src/workflow/engine.test.ts</files>
  <behavior>
    - Test: GA (attorney-required) offer accepted -> routes to attorney_review state
    - Test: CA (title-company) offer accepted -> routes directly to inspection_period (no attorney_review)
    - Test: NY (customary-attorney) offer accepted -> routes to attorney_review state
    - Test: counter_offered event -> stays in counter_pending, then offer_accepted -> progresses
    - Test: offer_rejected -> reaches closed_lost final state
    - Test: full happy path (offer -> accept -> inspect -> disclosure -> close) reaches closed_won
    - Test: swapping config (GA vs CA) changes routing without machine code change (LEGL-06)
  </behavior>
  <action>
    Create `src/workflow/engine.ts` using XState 5 `setup()` API (NOT v4 patterns):
    - `setup({ types, actors, guards })` with:
      - Types: context (transactionId, state, config: StateWorkflowConfig, currentStep), events (OFFER_SUBMITTED, COUNTER_OFFERED, OFFER_ACCEPTED, OFFER_REJECTED, INSPECTION_COMPLETE, FINANCING_CLEARED, CLOSING_DISCLOSURE_SENT, CLOSED), input (transactionId, propertyState, config)
      - Guards: `requiresAttorney` (closingType === 'attorney-required'), `requiresCustomaryAttorney` (closingType === 'customary-attorney')
      - Actors: `scheduleDeadlines` (fromPromise — stub that resolves), `notifyAttorneyRequired` (fromPromise — stub)
    - `.createMachine(config)` with states: offer_submitted, counter_pending, attorney_review, inspection_period, financing_period, pending_closing, closed_won, closed_lost
    - offer_submitted.on.OFFER_ACCEPTED uses array with guards: requiresAttorney -> attorney_review, requiresCustomaryAttorney -> attorney_review, default -> inspection_period
    - Export `transactionMachine` and `createTransactionActor(transactionId, propertyState, config)`

    Write tests FIRST (RED), then implement (GREEN). Use `createActor` + `actor.send()` + `actor.getSnapshot().value` to assert state transitions.
  </action>
  <verify>
    <automated>npx vitest run src/workflow/engine.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>XState machine correctly routes attorney-required states through attorney_review, FSBO states through self-service path, and customary-attorney states through optional attorney step. Config swap test proves data-driven routing (LEGL-06).</done>
</task>

</tasks>

<verification>
npx vitest run src/workflow 2>&1 | tail -20
npx next build 2>&1 | tail -20
</verification>

<success_criteria>
- 10 state config files exist with correct typing
- XState machine routes correctly based on closingType guard
- All unit tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-engine-state-compliance/04-01-SUMMARY.md`
</output>
